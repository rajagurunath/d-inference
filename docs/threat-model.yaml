version: "1.0"
created: "2026-05-14"
# Partially re-verified 2026-09-03 against commit 5d400cf75: A-001, A-003, T-003
# and T-011 were corrected to the shipped design (the coordinator is a plaintext
# endpoint on every request; the provider X25519 key is per-process and never on
# disk) — see docs/architecture/security/encryption.md and identity-binding.md.
# Every other entry still reflects the 2026-05-14 review and has not been
# re-verified.
methodology: STRIDE
description: >
  Runtime threat model for the EigenInference / darkbloom decentralized GPU inference
  platform. Covers production only. Dev environment, nation-state adversaries, and
  the release supply chain are explicitly out of scope.

# ─────────────────────────────────────────────────────────────
# SCOPE
# ─────────────────────────────────────────────────────────────
scope:
  in_scope:
    - Consumer → Coordinator API (HTTPS / OpenAI-compatible)
    - Coordinator → Provider WebSocket
    - Coordinator local disk (batch inputs and outputs at rest)
    - Provider process vs provider operator (local Mac access)
    - Console UI browser surface
    - Coordinator → Apple MDM / MDA trust services
    - Admin → Coordinator admin API
    - Provider in-process MLX inference engine
    - Optional provider → privileged fan-helper XPC boundary and AppleSMC writes
    - Coordinator → Stripe payment network
    - Apple attestation trust chain (cross-cutting)
  out_of_scope:
    - Dev / staging environment (api.dev.darkbloom.xyz)
    - GitHub Actions / R2 release supply chain
    - Nation-state / physical lab attacks on Apple Silicon
    - Denial-of-service via laptop shutdown (accepted operational risk)
    - Anthropic / Apple server-side infrastructure beyond MDM/MDA APIs

# ─────────────────────────────────────────────────────────────
# ADVERSARIES
# ─────────────────────────────────────────────────────────────
adversaries:
  - id: ADV-001
    name: malicious_provider
    description: >
      A Mac owner running the darkbloom provider CLI who wants to exfiltrate consumer
      prompt content, fake attestation to appear trusted, serve manipulated model
      outputs, or steal payment. Has physical and OS-level access to the host machine
      but cannot bypass SIP, Hardened Runtime, or the Secure Enclave hardware boundary.

  - id: ADV-002
    name: malicious_consumer
    description: >
      An authenticated or unauthenticated consumer who wants free inference, wants to
      enumerate or attack providers, manipulate billing, or extract information about
      the platform. Has access to the OpenAI-compatible API and console UI with a
      valid or stolen API key.

  - id: ADV-003
    name: external_attacker
    description: >
      An unauthenticated external party targeting the coordinator HTTP API, console UI,
      MDM webhook, or WebSocket endpoint. No prior access assumed. Goals include
      account takeover, SSRF, DoS, trust escalation, or payment manipulation.

# ─────────────────────────────────────────────────────────────
# ASSETS
# ─────────────────────────────────────────────────────────────
assets:
  - id: A-001
    name: consumer_prompts_completions
    description: Plaintext inference requests and responses from consumers.
    sensitivity: critical
    notes: >
      Sealed to the provider's X25519 key on the coordinator→provider leg (fresh
      session key pair per request) and to the coordinator's per-request session key
      on the provider→coordinator leg. The coordinator is a plaintext endpoint on
      every request: it decrypts in memory to parse, route, bill and re-encrypt, and
      does not log or persist content. Optional sender sealing
      (Content-Type application/eigeninference-sealed+json) protects the
      consumer→coordinator leg from TLS-terminating intermediaries; it does not hide
      content from the coordinator. Product copy claiming otherwise is SEC-015.
      Canonical description: docs/architecture/security/encryption.md.

  - id: A-002
    name: provider_se_p256_key
    description: Secure Enclave P-256 key used to sign attestation blobs and challenges.
    sensitivity: critical
    notes: Hardware-bound, non-exportable. Cannot leave the SE silicon.

  - id: A-003
    name: provider_x25519_key
    description: X25519 Diffie-Hellman private key used for E2E encryption of inference traffic.
    sensitivity: critical
    notes: >
      Generated in memory for each provider process (NodeKeyPair.generate() in the
      ProviderLoop initialiser; legacy on-disk key files are purged at start) and
      bound to the Secure Enclave identity through the signed blob's
      encryptionPublicKey. Never written to disk. Compromise of a running process
      exposes traffic for that process lifetime only. See
      docs/architecture/security/identity-binding.md.

  - id: A-004
    name: model_weights
    description: LLM weight files cached at ~/.cache/huggingface on the provider machine.
    sensitivity: high
    notes: >
      Not cryptographically protected at rest. An operator can replace weights; the
      coordinator's weight-hash verification is currently fail-open (SEC-007).

  - id: A-005
    name: coordinator_release_api_key
    description: Secret API key that authorises release registration at POST /v1/releases.
    sensitivity: critical
    notes: Controls which binary hashes are trusted for provider connections.

  - id: A-006
    name: coordinator_encryption_mnemonic
    description: BIP39 mnemonic used to derive the coordinator X25519 request-decryption identity.
    sensitivity: critical
    notes: Injected through the root-only GCP production environment. It has no payment or Solana role.

  - id: A-007
    name: consumer_api_keys
    description: Long-lived coordinator API keys issued to consumers, stored in browser localStorage.
    sensitivity: high
    notes: No expiry, not re-bound to current Privy identity (SEC-003).

  - id: A-008
    name: provider_api_tokens
    description: Coordinator-issued tokens that providers use to register via WebSocket.
    sensitivity: high

  - id: A-009
    name: payment_ledger
    description: Internal micro-USD balance ledger in PostgreSQL.
    sensitivity: high
    notes: Atomic balance operations required; duplicate webhook risk (SEC-012).

  - id: A-010
    name: attestation_blob_and_mda_chain
    description: >
      SE-signed attestation blob (hardware info, binary hash, SIP/SecureBoot state) and
      the Apple MDA DER certificate chain signed by Apple Enterprise Attestation Root CA.
    sensitivity: critical
    notes: Basis of the hardware trust level. Integrity must be maintained end-to-end.

  - id: A-011
    name: provider_binary_hash_registry
    description: Set of SHA-256 hashes of blessed provider binaries registered with the coordinator.
    sensitivity: critical
    notes: Controls which provider binaries can connect and serve private requests.

  - id: A-012
    name: step_ca_private_key
    description: >
      HISTORICAL (removed 2026-07-03): step-ca CA private key formerly used to issue
      device TLS certs for the ACME device-attest-01 enrollment leg. The ACME leg and
      the step-ca sidecar have been removed; the coordinator no longer holds this asset.
    sensitivity: critical
    notes: >
      Retained as a record only. The ACME leg was never wired end-to-end and was removed
      on 2026-07-03; hardware trust is MDM SecurityInfo only. Any residual step-ca state
      on old persistent disks should be destroyed, not migrated.

  - id: A-013
    name: profile_signing_identity
    description: >
      Code-signing identity (Apple Developer ID Application .p12 — cert + private key)
      used to CMS-sign the /v1/enroll .mobileconfig. Injected via KMS as
      PROFILE_SIGNING_P12_B64 (+ PROFILE_SIGNING_P12_PASSWORD).
    sensitivity: critical
    notes: >
      Compromise lets an attacker sign profiles (and binaries) macOS treats as coming
      from the registered developer. Optional — when unset the coordinator serves
      profiles unsigned.

  - id: A-014
    name: provider_fan_control_authority
    description: >
      Root authority to place Apple Silicon fans into manual mode, set bounded
      RPM targets, and temporarily assert the global Ftst fan-control gate.
    sensitivity: high
    notes: >
      Dormant unless the operator explicitly runs sudo darkbloom fan enable.
      Misuse can reduce cooling or interfere with macOS thermal management.

  - id: A-015
    name: batch_inputs_outputs_at_rest
    description: >
      Consumer prompt bodies and generated responses belonging to a 24-hour batch,
      waiting on coordinator local disk between submission and download: uploaded
      JSONL input files, per-item request bodies, per-item results, and the
      assembled output/error files.
    sensitivity: critical
    notes: >
      The same content class as A-001, but with a lifetime measured in hours or
      days rather than the milliseconds of a synchronous request — the batch lane
      is the only place consumer prompt content is stored at all. Every object is
      NaCl-Box ciphertext under an X25519 key derived from the coordinator
      mnemonic with HKDF info eigeninference-coordinator-batchstore-v1
      (coordinator/store/sealedblob/blob.go); files 0600, directory 0700, written
      temp-file + fsync + rename. Postgres holds metadata only: the three batch
      tables carry no body, hash or prefix column. A consumer may supply
      result_public_key, after which results are sealed to that key and the
      coordinator cannot read them back. Canonical description:
      docs/architecture/batch-lane.md.

# ─────────────────────────────────────────────────────────────
# TRUST BOUNDARIES
# ─────────────────────────────────────────────────────────────
trust_boundaries:
  - id: TB-001
    name: consumer_to_coordinator
    description: >
      Consumer applications (OpenAI SDK, curl, console UI) call the coordinator's
      OpenAI-compatible HTTP API over TLS. Two credential types are accepted: long-lived
      coordinator API keys (eigeninference-* prefix) and short-lived Privy JWTs for
      interactive sessions. The coordinator is trusted; consumers are not.
    how_it_works: >
      Authentication is resolved in server.go:requireAuth. Tokens beginning with "eyJ"
      are treated as Privy JWTs and verified via ECDSA ES256 against a static PEM
      verification key (PRIVY_VERIFICATION_KEY; no JWKS fetch) with pinned issuer
      "privy.io", audience = the Privy app ID, and a required exp claim
      (auth/privy.go NewPrivyAuth / VerifyToken); all others are looked up in the
      store via ValidateKey(). On first
      Privy login the coordinator auto-provisions a user account by calling the Privy
      REST API. API keys are hashed before storage; raw tokens are never persisted.

      The middleware chain for inference endpoints is: requireAuth →
      rateLimitConsumer → sealedTransport → handler. rateLimitConsumer applies a
      per-account token bucket; the financial rate limiter (rateLimitFinancial) applies
      to balance-mutating endpoints. Both limiters default to nil (unlimited) unless
      SetRateLimiter / SetFinancialRateLimiter are called at startup.

      Sender-to-coordinator encryption is opt-in. When enabled (sealedTransport
      middleware, sender_encryption.go), request bodies are decrypted with the
      coordinator's long-lived X25519 key (detected by Content-Type
      application/eigeninference-sealed+json only) under a 16 MiB read cap.
      Request bodies are capped in tiers with http.MaxBytesReader:
      bodyLimitMiddleware applies a 64 MiB ceiling to every request
      (maxRequestBodyBytes, server.go); plaintext inference endpoints
      (chat/completions, responses, completions, messages) are capped at 16 MiB
      (maxInferenceBodyBytes, inference_preprocess.go — 413 on excess); the
      unauthenticated control-plane bodies (enroll, device code, admin) at 64 KiB
      (maxControlPlaneBodyBytes); and the MDM webhook at 1 MiB
      (maxMDMWebhookBodyBytes).

      HTTP server config (cmd/coordinator/main.go): ReadHeaderTimeout=5s,
      ReadTimeout=10s, WriteTimeout=0 (streaming), IdleTimeout=120s,
      MaxHeaderBytes=64 KB.
    current_limitations:
      - "API key validation uses a simple store lookup — not constant-time comparison.
        Allows timing attacks to enumerate valid key prefixes (server.go)."
      - "WriteTimeout=0 (required for streaming) means a slow-reading client can hold an
        inference connection open indefinitely; the tiered body caps bound memory per
        request but not the number of concurrent slow connections."
      - "Consumer rate limiter defaults to nil (unlimited) unless explicitly configured
        at deploy time. No IP-level rate limiting exists."
      - "POST /v1/device/code and POST /v1/device/token have no rate limits, enabling
        device code enumeration and provider token brute force (device_auth.go)."
      - "Device codes are not consumed atomically on first token exchange; the approved
        code can be polled multiple times to mint multiple provider tokens (device_auth.go)."
      - "Error responses distinguish 'not found' (404) from 'expired' (410) on device
        token polling, leaking information about code lifecycle (device_auth.go)."

  - id: TB-002
    name: coordinator_to_provider_websocket
    description: >
      Providers open an outbound WebSocket to the coordinator. The coordinator upgrades
      the connection, receives a RegisterMessage, and runs attestation verification.
      Critically, providers are added to the active registry before attestation completes;
      verification is asynchronous. The coordinator is trusted; providers are adversarial
      until fully attested.
    how_it_works: >
      The WebSocket upgrade (provider.go) uses InsecureSkipVerify: true — no origin
      validation. A hard 10 MB per-message read limit is applied (conn.SetReadLimit). No
      pre-authentication, rate limiting, or connection limit exists before the upgrade.

      On receiving a RegisterMessage the coordinator immediately calls registry.Register()
      (registry.Register) which adds the provider to the active registry. Attestation verification
      runs asynchronously after this. Registration validates: TPS values clamped to hard
      limits; hardware memory clamped to 0–1024 GB; models filtered against the catalog;
      X25519 public key decoded and checked for 32-byte length.

      Binary hash policy: if a policy is configured, the hash in the attestation must
      match a registered blessed hash or the provider is marked untrusted — but it
      remains in the registry. If no policy is configured, any binary passes. Weight
      hash enforcement is fail-open: omitting weight_hash never fails admission.

      The private-text routing gate (registry.go providerSupportsPrivateTextLocked)
      requires all of: PublicKey present, backend mlx-swift, EncryptedResponseChunks,
      RuntimeManifestChecked, ChallengeVerifiedSIP, current application evidence when
      a release policy is enforced, CodeAttested once code-attestation enforcement is
      active, and five PrivacyCapabilities fields (text_backend_inprocess,
      text_proxy_disabled, anti_debug_enabled, core_dumps_disabled, env_scrubbed).
      The python_runtime_locked and dangerous_modules_blocked fields are
      wire-compatibility only and are not consulted. ChallengeVerifiedSIP is set by
      the coordinator after verifying a signed challenge response — not trusted from
      the provider. The gate is evaluated inside providerLivenessGateReasonLocked
      (routing_eligibility.go), which also requires a challenge verified within the
      last 16 minutes (challengeFreshnessMaxAge).

      Challenge-response runs immediately after registration and then every 5 minutes.
      The coordinator generates a 32-byte cryptographically random nonce, stores it in
      a one-time-use tracker (deleted on first response), and waits 30 seconds. The
      provider signs (nonce + timestamp) with the SE P-256 key; the coordinator verifies
      the ECDSA signature against the registered public key. Three consecutive failures
      mark the provider permanently untrusted; SIP=false or SecureBoot=false in any
      response triggers immediate untrust without the 3-strike rule.

      Status fields (SIPEnabled, BinaryHash, ActiveModelHash, etc.) may optionally be
      covered by a separate status_signature over a canonical JSON payload. For providers
      that omit status_signature, negative reports (SIP=false) are still acted on
      immediately, but positive reports are treated as advisory only.
    current_limitations:
      - "No pre-authentication rate limiting or per-IP connection caps before WebSocket
        upgrade. Any client can open arbitrary connections and attempt registration
        (provider.go, SEC-034)."
      - "Provider is added to the active registry before attestation verification
        completes. There is an async window where a provider with an invalid attestation
        is technically registered (provider.go registry.Register vs async verifyProviderAttestation)."
      - "Weight hash enforcement is fail-open: a provider can omit weight_hash entirely
        and still be admitted, even for models with a known expected hash in the catalog
        (registry.go, SEC-007)."
      - "status_signature is optional for pre-v0.3.11 providers. Positive status claims
        (SIP=true, BinaryHash=X) are not cryptographically bound for older providers,
        creating a rollout window where a compromised OS can echo a valid nonce while
        lying about its security posture (provider.go)."
      - "WebSocket frames after registration are not individually authenticated. Trust
        for the session is based entirely on the connection identity established at
        registration."
      - "10 MB per-message cap is global with no per-provider send/receive quota."

  - id: TB-003
    name: provider_operator_vs_process
    description: >
      The human operator who owns the Mac has full OS-level access: filesystem, process
      list, network interfaces. The darkbloom process must remain secure against this
      adversary. The design assumes SIP and Hardened Runtime are active (enforced at
      startup and via 5-minute coordinator challenges). The Secure Enclave provides a
      hardware boundary that even the operator cannot cross.
    how_it_works: >
      PT_DENY_ATTACH is applied at process startup via a raw ptrace(PT_DENY_ATTACH, 0,
      nil, 0) syscall (AntiDebug.swift denyDebuggerAttachment). While SIP is active
      this blocks debugger attachment even from root. Any non-zero result is fatal —
      SecurityError.ptDenyAttachFailed ("refusing to continue without anti-debug
      protection") with no environment override; checkDebuggerAttached reads P_TRACED
      via sysctl as a belt-and-suspenders check. At the same point the process
      disables core dumps (setrlimit RLIMIT_CORE=0) and EnvironmentScrubber unsets
      13 loader/malloc-debug variables (DYLD_INSERT_LIBRARIES, DYLD_LIBRARY_PATH,
      DYLD_FRAMEWORK_PATH, LD_PRELOAD, MallocStackLogging,
      MallocStackLoggingNoCompact, MallocScribble, MallocGuardEdges, MallocLogFile,
      MallocErrorAbort, NSZombieEnabled, OBJC_DEBUG_POOL_ALLOCATION,
      CFNETWORK_DIAGNOSTICS).

      SIP is checked once at startup by spawning /usr/bin/csrutil status and parsing for
      "enabled" (SecurityHardening.swift). If disabled the process aborts. The
      check is not repeated after startup; it is sound because disabling SIP requires a
      reboot into Recovery Mode which kills the process and purges inference data.

      The binary is SHA-256 hashed at startup via _NSGetExecutablePath() with symlink
      resolution, read in 64 KB chunks (BinaryHasher.swift). The hash is included in the
      SE-signed attestation blob and verified by the coordinator against its registry of
      blessed hashes. The hash is computed once at startup and not re-verified at runtime.

      The SE P-256 key is now persistent and stored in the macOS keychain via
      the Security framework (PersistentEnclaveKey.swift). On first launch the key is
      created with kSecAttrIsPermanent=true, bound to the keychain access group
      SLDQ2GJ6TL.io.darkbloom.provider (team-scoped), and labeled
      io.darkbloom.provider.attestation-signing.v2 (the current defaultLabel; a
      legacy v1 key from earlier builds is auto-migrated to v2 on load). Subsequent
      launches load the existing key by access group and label query. The private key
      never leaves the SE silicon. Two operations are performed with it: (1) ECDSA
      signing of attestation blobs, and (2) ECIES unwrap/decryption — when the
      default EngineV2 SSD cache is active (unless the single
      DARKBLOOM_PREFIX_CACHE=0 kill switch is set), the
      same key decrypts the wrapped per-provider KEK that protects on-disk KV
      (PersistentEnclaveKey+ECIES.swift, SecureEnclaveKeyWrappingService.swift). This
      grants entitled same-team binaries a decryption capability on this key, not only
      signing — the keychain item to audit is the v2 label above. ProviderLoop tries
      the persistent key first and falls back to an ephemeral CryptoKit key with a
      warning if the persistent path fails (e.g. errSecMissingEntitlement=-34018, which
      fires when a patched or unsigned binary lacks the keychain-access-groups
      entitlement, effectively preventing impostor binaries from using the established
      identity). Note: with the ephemeral fallback the KV cache cannot persist across
      restarts and is disabled rather than using a non-persistent key.

      The X25519 encryption key pair is generated ephemerally in memory using libsodium
      (NodeKeyPair.swift). It is NOT persisted to disk. Legacy on-disk key paths
      (~/.darkbloom/node_key and equivalents) are actively purged via purgeLegacyFiles()
      on startup. The secret key exists only in process memory, protected by Hardened
      Runtime (kernel denies task_for_pid).

      The provider config file (~/.config/darkbloom/provider.toml) stores only
      operational settings (coordinator URL, backend port, idle timeout, model list).
      It contains no cryptographic secrets. Core dumps are disabled (RLIMIT_CORE=0).
      Memory wiping functions (secureZero via memset_s) are defined in
      SecurityHardening.swift but call-site coverage in the inference path is not
      confirmed from static analysis.
    current_limitations:
      - "Binary hash is computed once at startup. A binary modification after the hash
        is captured (e.g. via a writable path the binary was loaded from) would not be
        detected until the next coordinator challenge (BinaryHasher.swift)."
      - "SIP check is single-shot at startup. This is sound given the Recovery Mode
        reboot requirement, but there is no in-process re-verification between coordinator
        challenges."
      - "secureZero* (memset_s) functions are defined but their invocation in the actual
        inference data path (decrypted prompt buffers, KV-cache tensors) is not confirmed
        by static analysis of SecurityHardening.swift."
      - "GPU-side MLX buffers (Metal) are not explicitly zeroed between requests.
        Residual prompt tokens and KV-cache data may persist in GPU memory until
        overwritten by the next batch."
      - "PT_DENY_ATTACH is applied early in startup, but a narrow race window exists on
        very fast process restart where a debugger could attach before the syscall fires
        (AntiDebug.swift)."
      - "The operator can read and replace model weight files at
        ~/.cache/huggingface. The coordinator's weight hash check is currently fail-open
        (SEC-007), so a substituted model may be served without detection."
      - "The persistent SE key (PersistentEnclaveKey.swift) is accessible to any binary
        presenting the same keychain access group (SLDQ2GJ6TL.io.darkbloom.provider) and
        label query. A legitimately code-signed binary from the same Developer ID team
        that includes the keychain-access-groups entitlement could load and sign with the
        established identity. The entitlement is team-scoped, so this does NOT apply to
        third-party binaries; it is limited to Apple-Developer-ID-signed code from team
        SLDQ2GJ6TL."
      - "errSecMissingEntitlement (-34018) fallback to an ephemeral key means a patched
        binary silently continues to operate rather than refusing to start. The ephemeral
        fallback key will differ from the registered public key, causing coordinator
        attestation verification to fail, but the failure occurs at the coordinator — the
        binary itself does not halt on keychain access failure."

  - id: TB-004
    name: browser_to_coordinator
    description: >
      The console-ui Next.js frontend runs in the browser and communicates with the
      coordinator through server-side API routes (BFF). Secrets stored in localStorage
      and the BFF's willingness to accept a client-supplied coordinator URL are the
      primary attack surfaces.
    how_it_works: >
      The coordinator API key is stored in localStorage as darkbloom_api_key in
      plaintext (useAuth.ts). On first Privy login the key is auto-provisioned via
      /api/auth/keys and persisted. The Privy JWT is managed by the Privy SDK in its
      own storage. The coordinator URL is stored in darkbloom_coordinator_url but is
      explicitly cleared on login (useAuth.ts) to prevent a pre-injection attack
      where an attacker pre-loads a malicious coordinator URL before the victim logs in.

      On logout (useAuth.ts) the following are cleared: darkbloom_api_key,
      eigeninference_api_key (legacy), darkbloom_coordinator_url, and the Privy session.
      The Zustand chat store, persisted to localStorage under darkbloom-store
      (store.ts), is NOT cleared on logout. Chat history and prior messages
      remain readable in localStorage after logout.

      Sender-to-coordinator encryption is controlled by a localStorage boolean flag
      (darkbloom_encrypt_to_coordinator, encryption.ts), defaulting to false. When
      enabled, each request uses a fresh ephemeral X25519 keypair and XSalsa20-Poly1305.
      If encryption setup fails the request hard-fails with a user-facing error; there
      is no silent plaintext fallback at the browser layer.

      Several Next.js BFF routes read the coordinator URL from the x-coordinator-url
      request header and use it for server-side fetches, forwarding Privy tokens or API
      keys to that origin (payments/stripe/checkout/route.ts, and four other payment
      routes, telemetry/route.ts). The inference and auth routes use the hardcoded
      NEXT_PUBLIC_COORDINATOR_URL environment variable and do not accept a
      client-supplied URL.

      next.config.ts contains only TypeScript build settings (ignoreBuildErrors: true).
      No security headers (CSP, X-Frame-Options, HSTS, Referrer-Policy, Permissions-Policy)
      are configured at the application layer.
    current_limitations:
      - "Chat history (Zustand store) is persisted to localStorage and not cleared on
        logout. A subsequent user of the same browser can read prior session prompts and
        responses (store.ts, SEC-024)."
      - "Five Next.js BFF routes accept x-coordinator-url from the client request header
        and use it for authenticated server-side fetches. This is a server-side request
        forgery primitive: an attacker with XSS, a malicious extension, or access to the
        same browser session can redirect payment and telemetry calls — including
        forwarded auth credentials — to an attacker-controlled host
        (payments/stripe/checkout/route.ts, SEC-001, SEC-002)."
      - "No Content-Security-Policy, X-Frame-Options, HSTS, or Referrer-Policy headers
        are configured at the application layer. XSS impact is unrestricted (next.config.ts,
        SEC-014)."
      - "Sender-to-coordinator sealing is opt-in and defaults off, and the coordinator
        decrypts every request in memory regardless. Product copy claiming all prompts
        are end-to-end encrypted is wrong (encryption.ts, lib/chat/stream.ts, SEC-015)."
      - "next.config.ts sets ignoreBuildErrors: true, allowing TypeScript errors to be
        silently ignored at build time, potentially masking type-safety regressions in
        security-sensitive code paths."
      - "Long-lived API keys in localStorage are not re-bound to the current Privy
        identity. A leaked key remains valid until explicitly revoked (SEC-003)."

  - id: TB-005
    name: coordinator_to_apple_mdm_mda
    description: >
      The coordinator upgrades providers from self-signed to hardware trust level by
      independently querying Apple's MDM and MDA services. MicroMDM sends SecurityInfo
      data to the coordinator via an inbound webhook. The coordinator also sends an SE
      public key hash as a nonce to Apple and receives back a DER-encoded certificate
      chain signed by Apple's Enterprise Attestation Root CA.
    how_it_works: >
      MDM webhook (server.go HandleMDMWebhook, POST /v1/mdm/webhook): the body is read
      through http.MaxBytesReader at 1 MiB (maxMDMWebhookBodyBytes). When
      EIGENINFERENCE_MDM_WEBHOOK_SECRET is set, the request must carry the secret in the
      X-Webhook-Token header or the ?token= query parameter, compared with
      subtle.ConstantTimeCompare (mdmWebhookTokenValid); when it is unset the webhook is
      unauthenticated. There is no mTLS or IP allowlist. mdmClient.HandleWebhook()
      correlates the callback's CommandUUID against the outstanding commands the
      coordinator itself issued (outstandingCommandTTL 30 min) and processes only the
      read-only request types SecurityInfo and DeviceInformation; uncorrelated callbacks
      are dropped and never matched by UDID alone. The handler logs body_size and a
      500-byte body_preview of the plist at Debug level. MicroMDM credentials are used
      only for outbound API calls to MicroMDM, not to authenticate inbound callbacks.

      When a provider registers, the coordinator's store-backed MDM scheduler sends a
      SecurityInfo MDM command to the enrolled device (mdm.go
      VerifyProviderWithUDIDObserver), then waits up to 90 seconds for the webhook
      response; a late SecurityInfo for the same CommandUUID is still applied
      (provider.go ApplyLateSecurityInfo). The webhook parses
      SecurityInfo fields from the plist response: SIPEnabled, SecureBootLevel,
      AuthenticatedRootVolumeEnabled, IsRecoveryLockEnabled, FirewaultEnabled.
      These are cross-checked against the provider's self-reported attestation values.
      A match on SIPEnabled and SecureBoot triggers async promotion to TrustHardware
      (provider.go).

      MDA verification (provider.go): the coordinator computes
      sha256(SE_public_key_bytes) and sends this as a nonce to Apple via
      SendDeviceAttestationCommand(). Apple returns a plist containing DER-encoded
      certificate chain entries. The coordinator parses the chain, verifies it against
      the embedded Apple Enterprise Attestation Root CA (P-384, valid 2022–2047) using
      Go's standard x509.Verify() with ExtKeyUsageAny. Custom OIDs are extracted from
      the leaf certificate: device serial number (1.2.840.113635.100.8.9.1), UDID
      (.8.9.2), OS version (.8.10.1), SepOS version (.8.10.2), Secure Boot level
      (.8.13.2), and freshness code (.8.11.1). The freshness code is compared against
      sha256(SE_public_key_bytes) byte-for-byte (provider.go).

      The SE attestation blob (attestation.go) is a JSON structure
      containing: SE public key (raw 64-byte P-256 X||Y in base64), hardware info, SIP
      and SecureBoot state, X25519 encryption public key, binary hash, system volume
      hash, serial number, and an RFC3339 timestamp. It is signed with SE P-256 ECDSA
      (DER). The coordinator SHA-256 hashes the canonical sorted JSON bytes and verifies
      the signature with ecdsa.Verify(). Three mandatory checks always run: SE must be
      available, SIP must be enabled, SecureBoot must be enabled. ARV status is parsed
      but not enforced (informational). RDMA status is informational only.
    current_limitations:
      - "The MDM webhook shared secret (EIGENINFERENCE_MDM_WEBHOOK_SECRET, X-Webhook-Token
        header or ?token=) is optional: when it is unset, any party that can reach
        POST /v1/mdm/webhook can post SecurityInfo or DevicePropertiesAttestation
        payloads. Injection is bounded by CommandUUID correlation — only a callback
        matching an outstanding command the coordinator issued (30-minute TTL) is
        processed — but a forged payload for a live CommandUUID could still grant
        hardware trust without genuine MDM enrollment. No mTLS or IP allowlist
        (server.go HandleMDMWebhook, SEC-004)."
      - "HandleMDMWebhook logs body_size and a 500-byte body_preview of the MDM plist at
        Debug level. MDM plists carry device identifiers (UDID, serial), so Debug-level
        coordinator logs are identity-bearing even though they never contain inference
        data."
      - "The MDA freshness code is sha256(SE_public_key_bytes). The SE public key remains
        visible in the privacy-redacted public attestation status. An attacker can
        pre-compute the expected freshness code, potentially forging a cert with a
        matching OID value."
      - "No OCSP/CRL checks on the Apple MDA certificate chain. A revoked Apple
        intermediate CA would be accepted until the embedded root is manually updated."
      - "x509.Verify() is called with ExtKeyUsageAny, which does not enforce leaf or
        intermediate key usage extensions (mda.go)."
      - "ARV (Authenticated Root Volume) and RDMA status are parsed from attestation and
        MDM data but explicitly not enforced by the trust state machine — old provider
        versions omit these fields."
      - "MDM verification and MDA verification are asynchronous. A provider is routable
        at TrustSelfSigned while both checks are pending, before any independent Apple
        verification has completed."

  - id: TB-006
    name: admin_to_coordinator
    description: >
      A distinct admin API surface controls release registration, binary hash registry,
      trust-level overrides, provider management, and platform configuration. It is gated
      by a separate admin key (constant-time compared) and by Privy admin email
      membership. The release key is a narrower credential for GitHub Actions that can
      only POST /v1/releases.
    how_it_works: >
      isAdminAuthorized() (release_handlers.go) first checks the admin key
      using subtle.ConstantTimeCompare(). If that fails it checks whether the Privy
      JWT user is in the configured admin email list. Admin key requests bypass the
      consumer and financial rate limiters entirely (the accountID is set to "admin"
      which hits no rate limit bucket). No per-admin audit log is emitted; all admin
      actions share the same "admin" identity in logs.

      The release key (release_handlers.go) is a separate secret checked with
      plain string equality (== operator), NOT subtle.ConstantTimeCompare(). It controls
      POST /v1/releases, which registers a new release, syncs binary hashes into the
      provider registry gate, and invalidates caches. After registration the cache key
      invalidated is latest_release:v1, but the actual per-platform cache key used for
      lookups is latest_release:v1:<platform> (SEC-021). This means installers and
      auto-updaters may continue receiving stale release metadata from the per-platform
      cache after a new release is registered or an old one is deactivated.

      The device auth flow (device_auth.go) has no rate limits on POST /v1/device/code
      (generates device codes) or POST /v1/device/token (polls for tokens). Approved
      device codes are not atomically consumed on first token exchange; any holder of
      the device code can continue polling and minting provider tokens during the 15-minute
      validity window.

      Admin OTP endpoints (POST /v1/admin/otp/init and /v1/admin/otp/verify) are
      unauthenticated and not rate-limited, creating a spam and brute-force surface
      (release_handlers.go).
    current_limitations:
      - "The release key is compared with plain string equality, not constant-time
        comparison. A timing attack can enumerate key bytes (release_handlers.go,
        SEC-009)."
      - "Cache invalidation on release registration and deletion targets the wrong key
        (latest_release:v1 instead of latest_release:v1:<platform>). Installers and
        providers on a specific platform may see stale release metadata for up to 1 minute
        after a security-critical release update (release_handlers.go, SEC-021)."
      - "Admin key bypasses all rate limiters. A compromised admin key has unconstrained
        API throughput (server.go)."
      - "A single shared admin key means all admin actions are attributed to the same
        identity. Individual admin revocation is not possible without rotating the single
        key (server.go)."
      - "No alerting or audit log entry on release registration, binary hash sync, or
        admin trust-level overrides. Malicious registration would be silent."
      - "Admin OTP endpoints are unauthenticated and unrate-limited (SEC-028)."
      - "Device codes are not atomically consumed on first token exchange, allowing
        multiple provider tokens to be minted from one approved code (device_auth.go,
        SEC-011)."

  - id: TB-007
    name: provider_inference_engine
    description: >
      The in-process MLX inference engine (mlx-swift-lm) executes inside the darkbloom
      process on the provider's Apple Silicon GPU. Decrypted prompt data is present in
      process memory during inference. Isolation from the OS and operator is provided by
      the process boundary, Hardened Runtime, and memory wiping. The inference engine
      is the innermost trust boundary — a failure here directly exposes consumer data.
    how_it_works: >
      ProviderLoop and StandaloneServer own one EngineV2Bridge per resident model.
      Each bridge wraps mlx-swift-lm ContinuousBatchingV2 with a configurable
      running-row cap (default 4, clamped to 1–8) and a bounded waiting queue
      (default 64). EngineV2Runtime is the
      process-wide registry used for aggregate capacity and cancellation. Every v0.7.5
      model slot uses this path; there is no legacy inference fallback.

      Model weights are loaded from the HuggingFace cache directory
      (~/.cache/huggingface) via ModelScanner.resolveLocalPath(). Weight integrity is
      verified by WeightHasher: all weight files (*.safetensors, *.npz, *.bin),
      config.json, and tokenizer files are sorted lexicographically, SHA-256 hashed
      individually in parallel, and the per-file digests are concatenated then hashed
      again to produce a single root hash. This hash is advertised to the coordinator at
      registration and in heartbeats. The coordinator's enforcement of this hash is
      currently fail-open (SEC-007).

      Cancellation is managed via InferenceCancellationToken (NSLock-protected boolean).
      The token is checked between token generation steps in the streaming loop
      (ProviderLoop.swift). When cancelled the provider sends an inference_error
      response and stops generation. An active GPU forward pass is not interrupted —
      cancellation is checked only at the Swift layer between MLX calls.

      Response encryption uses the consumer's ephemeral X25519 public key via NaCl
      crypto_box (XSalsa20-Poly1305). Shared-key precomputation or per-chunk encryption
      failure sends inference_error and aborts the stream; plaintext response chunks are
      never emitted. The coordinator independently rejects plaintext chunks.

      Idle timeout requires a resident model, no in-flight requests, and elapsed time
      at or above the configured timeout. On unload, ProviderLoop unregisters and
      gracefully shuts down the model's EngineV2Bridge, removes the ModelSlot, and
      clears MLX's cache before re-slicing the surviving engine grants.

      GPU memory metrics (MLX.GPU.activeMemory, peakMemory, cacheMemory) are tracked
      and reported but not explicitly zeroed between requests. There is no Metal-level
      memset of GPU buffers between inference sessions.
    current_limitations:
      - "GPU-side MLX buffers (Metal allocations for activations, KV-cache, logits) are
        not explicitly zeroed between requests. Residual inference data from one consumer
        may be readable by a subsequent consumer's batch if MLX reuses allocations."
      - "Cancellation does not interrupt an active GPU forward pass. The MLX computation
        continues to completion before the Swift cancellation check fires, wasting
        provider resources and delaying billing reconciliation."
      - "The idle timeout monitor polls every 60 seconds, meaning a model can remain
        resident in GPU memory for up to 60 seconds beyond the configured timeout."
      - "Weight hash enforcement is fail-open at the coordinator (SEC-007). An operator
        who replaces model weight files will cause a hash mismatch in the next heartbeat,
        but omitting the hash entirely passes both registration and challenges."
      - "Model weight files are loaded from the user-writable HuggingFace cache directory.
        The operator can replace weight files between idle periods without affecting the
        currently-loaded model."

  - id: TB-008
    name: coordinator_to_payments
    description: >
      The coordinator manages an internal micro-USD ledger backed by PostgreSQL,
      processes Stripe webhook events for deposits, and uses Stripe Connect for
      provider payouts.
    how_it_works: >
      Stripe webhook ingestion (billing_handlers.go): the body is read with a
      1 MB limit (io.LimitReader). The Stripe-Signature header is verified using the
      Stripe SDK's VerifyWebhookSignature before any processing. Idempotency is checked
      by looking up the billing_session_id from the event metadata in the store and
      returning 200 if status == "completed". The status check and the credit insertion
      are not in a single database transaction; a concurrent duplicate webhook event can
      pass the completed-session check before either marks it complete, resulting in
      double credit (billing_handlers.go, SEC-012).

      The idempotency key used is a custom billing_session_id stored in Stripe metadata,
      not Stripe's own event.id. There is no unique database constraint on the
      billing_session_id that would enforce at-most-once credit at the DB level.

      The coordinator runs inside a GCP Confidential VM that reports AMD SEV,
      providing hardware-encrypted memory protection.
    current_limitations:
      - "Stripe webhook idempotency is not atomic: the completed-session check and the
        balance credit insertion are separate operations. Concurrent duplicate valid
        webhooks can both pass the check before either commits, risking double credit
        (billing_handlers.go, SEC-012)."
      - "Idempotency uses a custom billing_session_id rather than Stripe's native
        event.id. There is no unique database constraint enforcing at-most-once credit
        at the storage layer."
      - "No rate limiting on payment endpoints beyond the financial rate limiter, which
        defaults to nil (unlimited) unless explicitly configured."

  - id: TB-009
    name: apple_attestation_chain
    description: >
      The hardware trust guarantee rests on a chain: SE key generation → attestation
      blob construction and SE signing → coordinator signature verification → MDM
      SecurityInfo cross-check (the sole hardware grant) → MDA cert chain from Apple
      (the separate mda_verified flag) → ongoing challenge-response.
      This boundary is cross-cutting; a weakness at any link collapses the trust level
      of every provider in the field.
    how_it_works: >
      SE key lifecycle: PersistentEnclaveKey.swift creates or loads a persistent
      SecureEnclave P-256 signing key stored in the macOS keychain under access group
      SLDQ2GJ6TL.io.darkbloom.provider (team ID prefix) with label
      io.darkbloom.provider.attestation-signing.v2 (legacy v1 keys are migrated to v2
      on load) and kSecAttrIsPermanent=true.
      The same public key is reused across restarts, giving the provider a stable
      hardware-bound identity. The private key never leaves the SE silicon. The public
      key (raw 64-byte X||Y without 0x04 prefix, base64-encoded) is bound to the X25519
      encryption key in the attestation blob and in the coordinator registration message.
      Key rotation occurs only when the operator explicitly deletes the keychain item
      (e.g. after a factory reset or device wipe); routine process restarts preserve
      the established identity.

      Attestation blob: AttestationBuilder.swift constructs a JSON structure containing
      hardware info, SIP/SecureBoot/ARV state, the SE public key, the X25519 encryption
      public key, the binary SHA-256 hash, system volume hash, serial number, and an
      RFC3339 timestamp. Keys are serialized in sorted order to match Go's
      encoding/json field ordering. The blob is signed with SE P-256 ECDSA (DER-encoded).

      Coordinator verification (attestation.go): the raw P-256 public key is
      decoded (raw 65-byte uncompressed format), validated on the P-256 curve, then used
      to verify the DER ECDSA signature over SHA-256(canonical_json_bytes). Three checks
      are mandatory and will fail the attestation: SecureEnclave not available, SIP
      disabled, SecureBoot disabled. ARV and RDMA are parsed and logged but not enforced.
      For providers >= 0.8.15 (minProviderVersionForReconnectAttestation) the blob
      timestamp must be within ±2 minutes of coordinator time
      (RegistrationAttestationMaxAge, CheckTimestamp()) or the provider is marked
      untrusted; older providers' blobs are accepted with ChipFamily,
      RuntimeCapabilities, and MetallibHash stripped.

      Challenge-response: every 5 minutes the coordinator generates a 32-byte random
      nonce (base64), stores it in a one-time-use map keyed by nonce string, and sends
      it with a UTC RFC3339 timestamp. The provider signs (nonce + timestamp) with the
      SE key — raw string concatenation with no delimiter. The coordinator verifies the
      ECDSA signature, checks nonce matches, and checks that SIPEnabled is not nil and
      not false. SIP=false (and other security failures: bad signature, hash mismatch,
      impersonation) trigger immediate, non-recoverable MarkUntrusted(). 3 consecutive
      challenge *timeouts* (sleep / network blip) trigger MarkUntrustedTransient() via the
      3-strike counter: the provider is derouted but the challenge loop keeps probing it,
      and the next fully passing challenge restores it to online (RecordChallengeSuccess).
      Signature/SIP/hash failures are never auto-recovered and still require a reconnect.

      Status signature (attestation.go): an extended canonical JSON payload
      covering nonce, timestamp, and all security status fields is signed separately.
      VerifyStatusSignature() checks this signature. If the status_signature field is
      absent, ErrStatusSignatureMissing is returned; the caller treats this as advisory
      (not a failure) for compatibility with providers older than v0.3.11. Negative
      status reports (SIP=false) are always acted on immediately regardless of whether
      the status_signature is present.
    current_limitations:
      - "status_signature is optional for pre-v0.3.11 providers. A compromised provider
        running OS-level code can report a valid (nonce, timestamp) signature from the
        SE while reporting SIP=true in unsigned status fields, avoiding untrust during
        the rollout window (provider.go)."
      - "Nonce and timestamp are concatenated without a delimiter for the challenge
        signature (nonce + timestamp). A crafted nonce ending with a partial timestamp
        string could theoretically produce the same signed bytes as a different
        (nonce, timestamp) pair, though in practice base64 nonces (44 chars) and RFC3339
        timestamps are statistically non-overlapping."
      - "MDA freshness code is sha256(SE_public_key_bytes). The SE public key remains
        visible in the privacy-redacted GET /v1/providers/attestation status response.
        An attacker can pre-compute the expected freshness code for any known provider."
      - "No OCSP or CRL checks on the Apple Enterprise Attestation Root CA chain. A
        compromised or revoked Apple intermediate CA certificate would be accepted by the
        coordinator until the embedded root CA is manually updated."
      - "ARV (Authenticated Root Volume) status is parsed from both SE attestation and
        MDA OIDs but is explicitly not enforced in the trust state machine. Providers
        with a tampered system volume can still reach TrustHardware."
      - "MDM webhook authentication is an optional shared secret (see TB-005). Where it
        is unset, a forged SecurityInfo callback matching a live CommandUUID can cause a
        provider to reach TrustHardware without genuine Apple MDM enrollment, which is
        a prerequisite for the MDA step."
      - "The entire MDM→MDA trust upgrade path is asynchronous and runs in a background
        goroutine. A provider is routable at TrustSelfSigned during the window between
        registration and completion of the full verification chain."

  - id: TB-010
    name: provider_to_privileged_fan_helper
    description: >
      An optional root LaunchDaemon owns the AppleSMC fan-write connection. The
      unprivileged provider may only renew a bounded activity lease or read status;
      root may additionally request automatic restoration. The helper is outside
      the user-writable provider install tree.
    how_it_works: >
      Explicit sudo enable verifies the separately signed nested helper and atomically
      copies it to /Library/PrivilegedHelperTools/io.darkbloom.fan-helper, with a
      root-owned LaunchDaemon plist and validated JSON policy. Both XPC peers use
      NSXPCConnection code-signing requirements that pin Team SLDQ2GJ6TL and the
      io.darkbloom.provider / io.darkbloom.fan-helper identifiers. The helper also
      requires the configured numeric provider UID. The provider renews every five
      seconds; expiry after fifteen seconds restores automatic mode. The root helper
      exposes no arbitrary key/value SMC API and has no network, model, credential,
      or prompt dependency. Before any manual-mode write it fsyncs a root-owned
      ownership journal; launchd restart restores those fan indices before accepting
      another lease.
    current_limitations:
      - "Apple does not publish stable GPU sensor names or fan-write semantics. Active
        support is capability-gated by an explicit M1-M5 catalog and requires real-device
        validation for each model family."
      - "No process can catch SIGKILL. Safety after SIGKILL depends on launchd KeepAlive
        restart plus the durable ownership journal; simultaneous helper death and
        launchd disablement can leave manual state until reboot or administrator repair."
      - "The root-installed helper is not replaced by the unprivileged self-updater.
        Protocol mismatch intentionally leaves it dormant until the operator reruns
        sudo darkbloom fan enable."

  - id: TB-011
    name: coordinator_local_disk
    description: >
      The batch lane stores consumer prompt bodies and generated responses on the
      coordinator's own filesystem for the life of a batch, and its output files
      for seven days after that. Every other coordinator surface holds prompt
      content only in memory for the duration of one request. The boundary is
      between the coordinator process (trusted) and anything that can read that
      filesystem without going through the process: a disk image, a snapshot, a
      backup, a misconfigured volume mount, or root on the CVM host.
    how_it_works: >
      Blob store: coordinator/store/sealedblob/blob.go. Every object is sealed
      with NaCl Box to a long-lived X25519 key using a fresh ephemeral key pair,
      and stored as the JSON encoding of e2e.EncryptedPayload
      (ephemeral_public_key, ciphertext). Files are written 0600 into a 0700
      directory (EIGENINFERENCE_BATCH_BLOB_DIR, default
      /mnt/disks/userdata/batch) via temp file + fsync + rename, so a crash
      mid-write leaves no truncated blob. Blob refs are validated against
      [A-Za-z0-9_-]{1,128} and are always coordinator-minted row ids, never a
      consumer custom_id or filename, so consumer text can never become a path.

      Key: sealedblob.DeriveKey(mnemonic) with HKDF info
      eigeninference-coordinator-batchstore-v1 — the same construction and the
      same root secret as internal/e2e's sender-encryption key, under a separate
      domain, so the two keys are distinct and neither opens the other's
      ciphertext. With no mnemonic the lane refuses to start
      (api.NewBatchBlobStore returns nil, every /v1/files and /v1/batches route
      answers 503 batch_unavailable, and cmd/coordinator's startBatchDispatcher
      does not run), unless EIGENINFERENCE_BATCH_DEV_INSECURE_KEY=true selects a
      process-local random key for local development, logged as a WARN.

      Database: the three batch tables (batch_files, batches, batch_items) are
      metadata only — ids, states, counts, token counts, timestamps, blob refs.
      No column carries a body, a hash of one, or a prefix of one;
      TestNoContentColumnsInBatchTables pins that against
      information_schema.columns. Duplicate custom_ids are rejected in Go before
      any insert so a unique-index violation cannot print one into the Postgres
      server log.

      Plaintext lifetime: an uploaded file's blob is deleted the moment
      POST /v1/batches has fanned it out into per-item blobs; item input blobs
      are deleted when the batch finalizes; item results and the assembled
      output/error files are deleted seven days later (api.BatchOutputRetention,
      swept by coordinator/batchlane's retention pass and
      api.PurgeExpiredBatchFiles). An hourly orphan pass deletes item blobs no
      row references. Plaintext exists only inside batchlane's runItem, for the
      duration of one dispatch.

      Consumer-held key: a create carrying result_public_key (base64, exactly 32
      bytes, strictly validated) makes every result blob a PutTo seal to that
      key. The coordinator can then only Raw the blob for download, never Open
      it, and the output file's response.body is an e2e.EncryptedPayload object.
    current_limitations:
      - "The batch-store key is derived from the same mnemonic as the
        sender-encryption key. One root secret compromises both."
      - "The key is in coordinator process memory and the blobs are on the
        coordinator's own disk, so root on the CVM host can read both. The seal
        defends against disk images, snapshots and backups, not against the host."
      - "There is no key rotation mechanism. Rotating means redeploying with a new
        mnemonic, after which every existing blob is unreadable and every open
        batch is lost."
      - "EIGENINFERENCE_BATCH_DEV_INSECURE_KEY=true runs the lane on a
        process-local random key. It is logged loudly and is for local
        development only; set in production it would leave every batch
        unreadable after a restart while still accepting uploads."
      - "Batch metadata (Batch.Metadata), the uploaded filename and custom_id are
        consumer strings stored in Postgres by design. They are excluded from
        every log line and error message, but they are not sealed."

# ─────────────────────────────────────────────────────────────
# THREATS
# ─────────────────────────────────────────────────────────────
threats:

  # ── TB-001: Consumer → Coordinator ──────────────────────────

  - id: T-001
    trust_boundary: TB-001
    stride: Spoofing
    title: API key theft enables consumer impersonation
    description: >
      Long-lived coordinator API keys stored in browser localStorage can be stolen
      via XSS, malicious browser extensions, or local user access, and reused to
      impersonate the victim consumer for inference and billing operations.
    adversaries: [ADV-002, ADV-003]
    affected_assets: [A-007, A-009]
    affected_files:
      - console-ui/src/hooks/useAuth.ts
      - console-ui/src/lib/http/proxy-client.ts
      - console-ui/src/lib/migrate-storage.ts
      - coordinator/auth/**
    mitigations:
      - description: API keys verified on every coordinator request via constant-time compare.
        status: implemented
      - description: Keys stored in localStorage without HttpOnly/Secure cookie protection.
        status: open
    open_findings: [SEC-003]
    severity: high
    detection_hint: >
      Changes to API key issuance, validation, storage mechanism, or session lifecycle
      in auth handlers or the console-ui auth hook should be reviewed against this threat.

  - id: T-002
    trust_boundary: TB-001
    stride: Denial_of_Service
    title: Oversized request body causes coordinator OOM (resolved by tiered body caps)
    description: >
      The OpenAI-compatible inference endpoints used to read the full request body via
      io.ReadAll without a size cap, so an authenticated consumer could send a multi-GB
      body to exhaust coordinator memory. Bodies are now capped in tiers with
      http.MaxBytesReader: 64 MiB for every request (bodyLimitMiddleware,
      maxRequestBodyBytes), 16 MiB for plaintext inference bodies
      (maxInferenceBodyBytes, 413 on excess) and for sealed bodies, 64 KiB for the
      unauthenticated control-plane endpoints (maxControlPlaneBodyBytes), and 1 MiB
      for the MDM webhook (maxMDMWebhookBodyBytes).
    adversaries: [ADV-002, ADV-003]
    affected_assets: [A-009]
    affected_files:
      - coordinator/api/consumer.go
      - coordinator/api/inference_preprocess.go
      - coordinator/api/server.go
    mitigations:
      - description: >
          http.MaxBytesReader caps on every body: 64 MiB global (bodyLimitMiddleware),
          16 MiB plaintext inference (maxInferenceBodyBytes) and sealed
          (sender_encryption.go), 64 KiB control-plane, 1 MiB MDM webhook.
        status: implemented
    open_findings: []
    resolved_findings:
      - id: SEC-006
        resolution: >
          Closed: plaintext inference bodies are wrapped in http.MaxBytesReader at
          16 MiB (inference_preprocess.go maxInferenceBodyBytes) and every request is
          bounded by the 64 MiB bodyLimitMiddleware ceiling (server.go).
    severity: high
    detection_hint: >
      Any change to request body reading in consumer.go, inference_preprocess.go, or
      server.go (io.ReadAll, json.Decode, r.Body) should verify the MaxBytesReader
      caps remain in place at every tier.

  - id: T-003
    trust_boundary: TB-001
    stride: Information_Disclosure
    title: Coordinator is a plaintext endpoint for every request
    description: >
      The coordinator decrypts every request in memory (inside a confidential VM in
      production) to parse, route, bill and re-encrypt it, whether or not the consumer
      used sender sealing. Sender sealing is opt-in and off by default in the console;
      when used it protects only the consumer→coordinator leg from TLS-terminating
      intermediaries. Product copy that claims the coordinator never sees plaintext is
      wrong (SEC-015).
    adversaries: [ADV-003]
    affected_assets: [A-001]
    affected_files:
      - coordinator/api/sender_encryption.go
      - console-ui/src/lib/encryption.ts
      - console-ui/src/lib/chat/stream.ts
      - console-ui/src/app/api/chat/route.ts
      - console-ui/src/app/page.tsx
    mitigations:
      - description: Provider-leg encryption is always on (X25519/NaCl box, fresh session key per request).
        status: implemented
      - description: The coordinator does not log or persist request or response content (encryption.md, "What the coordinator logs and retains").
        status: implemented
      - description: Sender-to-coordinator sealing is opt-in and off by default; it does not remove the coordinator from the plaintext path.
        status: open
    open_findings: [SEC-015]
    severity: medium
    detection_hint: >
      Changes to encryption.ts defaults, the sealing path in lib/chat/stream.ts or
      app/api/chat/route.ts, sender_encryption.go, or product copy claiming E2E
      encryption should re-examine this boundary.

  - id: T-004
    trust_boundary: TB-001
    stride: Elevation_of_Privilege
    title: Consumer API key used for admin-scoped operations
    description: >
      If consumer and admin API key validation shares code paths without scope
      enforcement, a consumer key could be crafted or escalated to call admin
      endpoints.
    adversaries: [ADV-002]
    affected_assets: [A-005, A-011]
    affected_files:
      - coordinator/auth/**
      - coordinator/api/server.go
      - coordinator/api/release_handlers.go
    mitigations:
      - description: Admin endpoints gated by separate admin key middleware.
        status: implemented
      - description: Release key uses plain string equality rather than constant-time compare.
        status: open
    open_findings: [SEC-009]
    severity: high
    detection_hint: >
      Changes to middleware ordering, key validation logic, or new route registration
      in server.go should verify admin vs consumer key scopes are not merged.

  - id: T-005
    trust_boundary: TB-001
    stride: Repudiation
    title: Consumer denies inference requests and billing charges
    description: >
      A consumer disputes that they sent requests charged to their balance. Without
      immutable per-request logs tied to the API key, billing disputes cannot be
      resolved.
    adversaries: [ADV-002]
    affected_assets: [A-009]
    affected_files:
      - coordinator/api/consumer.go
      - coordinator/store/postgres.go
    mitigations:
      - description: Usage rows written per request to the ledger.
        status: implemented
      - description: No cryptographic receipt or signed request log.
        status: open
    open_findings: []
    severity: low
    detection_hint: >
      Changes to usage recording, ledger writes, or billing calculation should
      ensure every billable request is persisted before returning a response.

  # ── TB-002: Coordinator → Provider WebSocket ─────────────────

  - id: T-006
    trust_boundary: TB-002
    stride: Spoofing
    title: Unauthenticated WebSocket connection floods provider registry
    description: >
      The provider WebSocket endpoint accepts connections from any origin before
      authentication, registers providers into the active registry before trust is
      verified, and allows 10 MiB messages. An attacker can open many connections
      to exhaust goroutines, registry memory, and challenge-loop capacity.
    adversaries: [ADV-001, ADV-003]
    affected_assets: [A-011]
    affected_files:
      - coordinator/api/provider.go
      - coordinator/registry/registry.go
    mitigations:
      - description: Binary hash verified at registration against known blessed hashes.
        status: implemented
      - description: No per-IP connection rate limit or pre-auth token requirement.
        status: open
    open_findings: [SEC-034]
    severity: medium
    detection_hint: >
      Changes to the WebSocket upgrade path, provider registration flow, or registry
      admission logic should verify rate-limiting and pre-auth requirements.

  - id: T-007
    trust_boundary: TB-002
    stride: Tampering
    title: Provider serves manipulated model outputs to consumers
    description: >
      A malicious provider can run a modified model or post-process outputs before
      returning them. The consumer has no cryptographic proof that outputs came from
      the advertised model weights.
    adversaries: [ADV-001]
    affected_assets: [A-001, A-004]
    affected_files:
      - coordinator/registry/registry.go
      - coordinator/api/provider.go
      - provider-swift/Sources/ProviderCore/Inference/**
      - provider-swift/Sources/ProviderCoreFoundation/WeightHasher.swift
      - provider-swift/Tests/ProviderCoreFoundationTests/WeightHasherMemoryTests.swift
    mitigations:
      - description: Weight hash advertised by provider at registration and in heartbeats.
        status: implemented
      - description: Coordinator verification of weight hash is fail-open when hash is omitted (SEC-007).
        status: open
      - description: No consumer-side output proof (output signing not implemented).
        status: open
    open_findings: [SEC-007]
    severity: high
    detection_hint: >
      Changes to model hash advertisement, registry scoring, or the routing gate
      should check that weight hash enforcement is fail-closed for catalog models.

  - id: T-008
    trust_boundary: TB-002
    stride: Information_Disclosure
    title: Regression could expose plaintext SSE chunks on encryption failure
    description: >
      The provider must fail closed if shared-key setup or response encryption fails.
      Current Swift code sends inference_error and aborts without emitting plaintext;
      the coordinator also rejects plaintext chunks as an independent backstop.
    adversaries: [ADV-001]
    affected_assets: [A-001]
    affected_files:
      - provider-swift/Sources/ProviderCore/ProviderLoop+InferenceHandler.swift
      - coordinator/api/provider.go
    mitigations:
      - description: Coordinator rejects plaintext inference chunks and marks provider untrusted.
        status: implemented
      - description: Provider fails closed with inference_error on key setup or chunk-encryption failure.
        status: implemented
    open_findings: []
    severity: medium
    detection_hint: >
      Any change to the streaming response path in ProviderLoop.swift, encryption
      error handling, or the coordinator's chunk ingestion should verify the
      fail-closed behaviour is preserved.

  - id: T-009
    trust_boundary: TB-002
    stride: Elevation_of_Privilege
    title: Swift provider excluded from private-request routing due to missing Python flags (resolved)
    description: >
      The coordinator's private-text routing gate used to require Python-specific
      flags (python_runtime_locked, dangerous_modules_blocked) that the Swift provider
      always reports as false, which would have excluded Swift providers from routing
      or created pressure to lie about controls that do not exist in the Swift backend.
      providerSupportsPrivateTextLocked now requires only the five backend-neutral
      PrivacyCapabilities (text_backend_inprocess, text_proxy_disabled,
      anti_debug_enabled, core_dumps_disabled, env_scrubbed) plus backend mlx-swift;
      the two Python fields remain on the wire for compatibility and are not consulted.
    adversaries: [ADV-001]
    affected_assets: [A-001]
    affected_files:
      - provider-swift/Sources/ProviderCore/ProviderLoop.swift
      - coordinator/registry/registry.go
    mitigations:
      - description: Routing gate exists and enforces the five backend-neutral privacy flags.
        status: implemented
      - description: Python-only flags are wire-compatibility fields, not gate inputs.
        status: implemented
    open_findings: []
    resolved_findings:
      - id: SEC-017
        resolution: >
          Closed: registry.go providerSupportsPrivateTextLocked no longer reads
          PythonRuntimeLocked / DangerousModulesBlocked; Swift providers pass the gate
          with the five required capabilities.
    severity: medium
    detection_hint: >
      Changes to the provider registration message, registry routing gate, or
      backend-type handling should verify the gate keeps evaluating backend-neutral
      controls rather than reintroducing Python flag semantics.

  - id: T-010
    trust_boundary: TB-002
    stride: Denial_of_Service
    title: In-flight inference cancellation not propagated to inference engine
    description: >
      On a coordinator cancel message the provider must stop the in-flight
      inference task, or it keeps consuming GPU and memory after cancellation.
      This was an open gap in the now-removed legacy provider (SEC-020),
      which created a cancellation token but never wired it into the in-process
      task. The Swift provider wires InferenceCancellation into the generation
      loop; the residual "cannot interrupt an active GPU forward pass"
      limitation is documented in the TB-007 current limitations.
    adversaries: [ADV-002]
    affected_assets: [A-009]
    affected_files:
      - provider-swift/Sources/ProviderCore/ProviderLoop.swift
    mitigations:
      - description: Swift provider wires InferenceCancellation into the generation loop.
        status: implemented
    open_findings: []
    severity: medium
    detection_hint: >
      Changes to cancellation handling in ProviderLoop.swift should verify the
      cancel signal reaches the inference loop and aborts generation.

  # ── TB-003: Provider Operator vs Process ─────────────────────

  - id: T-011
    trust_boundary: TB-003
    stride: Information_Disclosure
    title: Provider operator reads X25519 private key from process memory
    description: >
      The X25519 Diffie-Hellman private key used for E2E encryption is generated in
      memory for each provider process and never written to disk (legacy key files
      are purged at start). An operator with root can still read the memory of the
      running process and recover the key for that process lifetime, decrypting the
      traffic routed to it during that time. Severity has not been re-rated since
      the on-disk storage was removed.
    adversaries: [ADV-001]
    affected_assets: [A-003, A-001]
    affected_files:
      - provider-swift/Sources/ProviderCore/Crypto/NodeKeyPair.swift
      - provider-swift/Sources/ProviderCore/ProviderLoop.swift
    mitigations:
      - description: Key is process-scoped and never persisted (NodeKeyPair.generate(), NodeKeyPair.purgeLegacyFiles()).
        status: implemented
      - description: XSalsa20-Poly1305 provides no forward secrecy within a process lifetime; a new key per process bounds exposure to that lifetime.
        status: implemented
      - description: Key material is not protected by the Secure Enclave; only the P-256 identity key is.
        status: open
    open_findings: []
    severity: high
    detection_hint: >
      Changes to key generation, storage, or loading in ProviderConfig.swift or
      config.rs should verify whether the X25519 private key is protected at rest
      (e.g. moved to macOS Keychain or derived from SE-sealed material).

  - id: T-012
    trust_boundary: TB-003
    stride: Tampering
    title: Operator replaces model weights with manipulated files
    description: >
      The operator has write access to ~/.cache/huggingface where model weights are
      stored. Replacing weights causes the provider to serve outputs from a different
      or adversarially fine-tuned model while advertising the original model's hash.
      The coordinator's weight-hash check is currently fail-open.
    adversaries: [ADV-001]
    affected_assets: [A-004, A-001]
    affected_files:
      - coordinator/registry/registry.go
      - coordinator/api/provider.go
      - provider-swift/Sources/ProviderCoreFoundation/WeightHasher.swift
      - provider-swift/Tests/ProviderCoreFoundationTests/WeightHasherMemoryTests.swift
    mitigations:
      - description: Provider computes and advertises weight hash at registration.
        status: implemented
      - description: Coordinator fails open when provider omits weight_hash for a catalog model (SEC-007).
        status: open
    open_findings: [SEC-007]
    severity: high
    detection_hint: >
      Changes to WeightHasher.swift, model registration, heartbeat hash fields, or
      the coordinator's registry admission should verify that hash enforcement is
      fail-closed for all catalog models.

  - id: T-013
    trust_boundary: TB-003
    stride: Tampering
    title: Operator modifies darkbloom binary
    description: >
      The operator attempts to replace or patch the darkbloom binary to bypass
      security checks, fake attestation, or exfiltrate data.
    adversaries: [ADV-001]
    affected_assets: [A-011, A-010]
    affected_files:
      - provider-swift/Sources/ProviderCore/Security/BinaryHasher.swift
      - coordinator/attestation/**
    mitigations:
      - description: Code signing enforced by macOS; SIP prevents unsigned binaries from launching.
        status: implemented
      - description: Binary SHA-256 hash included in SE-signed attestation blob, verified by coordinator.
        status: implemented
      - description: Coordinator maintains registry of blessed binary hashes; unregistered hashes rejected.
        status: implemented
      - description: >
          Persistent SE key access group (SLDQ2GJ6TL.io.darkbloom.provider) enforces that
          only binaries with the matching keychain-access-groups entitlement can load the
          established identity. A patched binary lacking the entitlement receives
          errSecMissingEntitlement (-34018) and falls back to an ephemeral key, which the
          coordinator will reject as the public key no longer matches the registered
          fingerprint (PersistentEnclaveKey.swift).
        status: implemented
    open_findings: []
    severity: critical
    detection_hint: >
      Changes to binary hash computation in BinaryHasher.swift, attestation blob
      construction, the coordinator's binary hash registry admission, or the keychain
      access group entitlement in the provisioning profile should preserve the
      end-to-end tamper-detection chain.

  - id: T-014
    trust_boundary: TB-003
    stride: Information_Disclosure
    title: Operator inspects darkbloom process memory
    description: >
      The operator attempts to attach a debugger or read process memory to extract
      decrypted prompt content, the X25519 key from memory, or SE-derived material.
    adversaries: [ADV-001]
    affected_assets: [A-001, A-003, A-002]
    affected_files:
      - provider-swift/Sources/ProviderCore/Security/AntiDebug.swift
      - provider-swift/Sources/ProviderCore/Security/SecurityHardening.swift
    mitigations:
      - description: PT_DENY_ATTACH applied at startup; kernel denies task_for_pid.
        status: implemented
      - description: Hardened Runtime entitlement blocks external memory inspection.
        status: implemented
      - description: SE P-256 key is non-exportable and never present in regular process memory.
        status: implemented
      - description: Memory wiping applied to inference buffers after each request.
        status: implemented
    open_findings: []
    severity: critical
    detection_hint: >
      Any weakening of AntiDebug.swift, removal of PT_DENY_ATTACH, changes to
      Hardened Runtime entitlements, or deferral of memory wiping should be
      treated as a critical regression.

  - id: T-015
    trust_boundary: TB-003
    stride: Elevation_of_Privilege
    title: Operator disables SIP to bypass security controls
    description: >
      The operator reboots into Recovery Mode to disable SIP, then restarts darkbloom
      to operate without code-signing and anti-debug protections.
    adversaries: [ADV-001]
    affected_assets: [A-001, A-010]
    affected_files:
      - provider-swift/Sources/ProviderCore/Security/SecurityHardening.swift
      - coordinator/api/provider.go
      - coordinator/registry/registry.go
    mitigations:
      - description: darkbloom checks SIP at startup and refuses to serve if disabled.
        status: implemented
      - description: SIP status included in every 5-minute challenge-response; coordinator marks untrusted immediately on false.
        status: implemented
      - description: Disabling SIP requires reboot into Recovery Mode, which terminates the process and wipes inference data from memory.
        status: implemented
    open_findings: []
    severity: critical
    detection_hint: >
      Changes to SIP-check logic in SecurityHardening.swift, the challenge-response
      handler, or the coordinator's untrust-on-SIP-false path are critical and should
      be reviewed against this threat.

  - id: T-016
    trust_boundary: TB-003
    stride: Denial_of_Service
    title: Operator fills disk to prevent model loading
    description: >
      The operator fills the local filesystem to prevent model weight files from
      being cached or loaded, degrading provider availability.
    adversaries: [ADV-001]
    affected_assets: []
    affected_files:
      - provider-swift/Sources/ProviderCore/InferenceFoundation/LocalMLXModelFoundation.swift
    mitigations:
      - description: Model load failure reported to coordinator; provider marked unavailable and re-queued.
        status: implemented
      - description: No disk quota enforcement for HuggingFace cache.
        status: open
    open_findings: []
    severity: low
    detection_hint: >
      Changes to model loading error handling should verify failures are reported
      upstream rather than silently retried.

  # ── TB-004: Browser / Console UI ─────────────────────────────

  - id: T-017
    trust_boundary: TB-004
    stride: Spoofing
    title: SSRF via client-controlled coordinator URL forwards auth credentials
    description: >
      Several Next.js API routes read the coordinator URL from the x-coordinator-url
      request header and forward Privy tokens or API keys to that origin. An attacker
      with XSS, extension access, or a malicious link can redirect authenticated
      server-side fetches to an attacker-controlled host.
    adversaries: [ADV-002, ADV-003]
    affected_assets: [A-007]
    affected_files:
      - console-ui/src/app/api/payments/stripe/checkout/route.ts
      - console-ui/src/app/api/payments/stripe/onboard/route.ts
      - console-ui/src/app/api/payments/withdraw/stripe/route.ts
      - console-ui/src/app/api/payments/stripe/status/route.ts
      - console-ui/src/app/api/payments/stripe/withdrawals/route.ts
      - console-ui/src/app/api/telemetry/route.ts
    mitigations:
      - description: No server-side allowlist for coordinator URLs in affected routes.
        status: open
    open_findings: [SEC-001, SEC-002]
    severity: high
    detection_hint: >
      Any route that reads a URL from a request header or body and uses it for a
      server-side fetch must be reviewed for SSRF and credential-forwarding risk.

  - id: T-018
    trust_boundary: TB-004
    stride: Information_Disclosure
    title: Chat history persists in localStorage after logout
    description: >
      Chat messages and prompt content remain in localStorage/Zustand after the user
      logs out. A subsequent local user, browser extension, or future XSS can read
      prior session prompts.
    adversaries: [ADV-003]
    affected_assets: [A-001]
    affected_files:
      - console-ui/src/lib/store.ts
      - console-ui/src/hooks/useAuth.ts
    mitigations:
      - description: Logout clears API keys and coordinator URL but not chat state.
        status: open
    open_findings: [SEC-024]
    severity: medium
    detection_hint: >
      Changes to logout logic (useAuth.ts) or Zustand persistence config (store.ts)
      should verify all sensitive state is cleared on account change.

  - id: T-019
    trust_boundary: TB-004
    stride: Information_Disclosure
    title: Missing security headers increase XSS blast radius
    description: >
      No CSP, frame-ancestors, X-Content-Type-Options, or Referrer-Policy headers
      are configured at the application layer. If a rendering or dependency bug
      introduces XSS, browser-stored API keys and chat data are unprotected.
    adversaries: [ADV-003]
    affected_assets: [A-007, A-001]
    affected_files:
      - console-ui/next.config.ts
      - console-ui/src/proxy.ts
    mitigations:
      - description: No application-layer security headers currently configured.
        status: open
    open_findings: [SEC-014]
    severity: medium
    detection_hint: >
      Changes to next.config.ts or proxy.ts headers config should verify CSP and
      other protective headers are present before deployment.

  - id: T-020
    trust_boundary: TB-004
    stride: Elevation_of_Privilege
    title: Mock auth active in production due to missing Privy config
    description: >
      If the Privy app ID env var is absent in production, the UI falls back to
      MOCK_AUTH which sets authenticated=true unconditionally. Coordinator API calls
      will still fail auth, but the UI presents an authenticated state that may
      expose unauthenticated flows.
    adversaries: [ADV-003]
    affected_assets: [A-007]
    affected_files:
      - console-ui/src/components/providers/PrivyClientProvider.tsx
    mitigations:
      - description: Mock auth only intended for local development.
        status: open
    open_findings: [SEC-026]
    severity: medium
    detection_hint: >
      Changes to PrivyClientProvider.tsx auth fallback logic should fail closed
      in production environments rather than granting a mock authenticated state.

  # ── TB-005: Coordinator → Apple MDM/MDA ──────────────────────

  - id: T-021
    trust_boundary: TB-005
    stride: Spoofing
    title: Unauthenticated MDM webhook injection manipulates provider trust state
    description: >
      The MDM webhook endpoint is authenticated only by an optional shared secret
      (EIGENINFERENCE_MDM_WEBHOOK_SECRET via X-Webhook-Token or ?token=); where the
      secret is unset, an attacker who can reach it can post SecurityInfo or
      DevicePropertiesAttestation callbacks. A forged callback is processed only if
      its CommandUUID matches a command the coordinator issued within the last
      30 minutes, but for such a live command it could elevate a provider's trust
      level to hardware without genuine MDM enrollment.
    adversaries: [ADV-001, ADV-003]
    affected_assets: [A-010]
    affected_files:
      - coordinator/api/server.go
      - coordinator/mdm/mdm.go
      - coordinator/api/provider.go
    mitigations:
      - description: Coordinator verifies MDA cert chain against Apple Enterprise Attestation Root CA.
        status: implemented
      - description: >
          Shared-secret check (X-Webhook-Token header or ?token=, constant-time compare)
          when EIGENINFERENCE_MDM_WEBHOOK_SECRET is set; CommandUUID correlation against
          outstanding commands (30-minute TTL) and a SecurityInfo/DeviceInformation-only
          allowlist in mdm.Client.HandleWebhook.
        status: implemented
      - description: Body capped at 1 MiB (maxMDMWebhookBodyBytes) via http.MaxBytesReader.
        status: implemented
      - description: Secret is optional (unset → unauthenticated); no mTLS or IP allowlist.
        status: open
    open_findings: [SEC-004]
    severity: high
    detection_hint: >
      Changes to MDM webhook handling (mdm.go, server.go route registration) must
      verify the shared-secret check, body cap, and CommandUUID correlation remain in
      front of any trust state change, and that deployments set the secret.

  - id: T-022
    trust_boundary: TB-005
    stride: Tampering
    title: ACME device identity spoofable via proxy header injection (resolved by removal)
    description: >
      RESOLVED BY REMOVAL (2026-07-03): ACME device identity used to be carried via
      X-Ssl-Client-* proxy headers read by api/acme_verify.go; a directly-reachable
      coordinator or a proxy that failed to strip these headers would have allowed a
      spoofed certificate identity. The entire ACME trust leg — acme_verify.go, the
      header parsing, and the trust grant — was deleted on 2026-07-03, eliminating
      this attack surface. Hardware trust is MDM SecurityInfo only.
    adversaries: [ADV-001, ADV-003]
    affected_assets: [A-010, A-012]
    affected_files:
      - coordinator/api/provider.go
    mitigations:
      - description: >
          ACME leg removed 2026-07-03; the coordinator no longer parses X-Ssl-Client-*
          headers and no trust path consumes proxy-supplied client-cert identity.
        status: implemented
      - description: >
          If a cert-based trust leg is ever reintroduced, carry the proof in-band over
          the provider WebSocket (never proxy headers) and fail closed on Apple
          SIP/SecureBoot posture OIDs.
        status: open
    open_findings: []
    resolved_findings:
      - id: SEC-005
        resolution: >
          Closed 2026-07-03: the header-parsing surface (acme_verify.go and every
          ACME trust-grant call site) was deleted; verified no X-Ssl-Client-*
          consumer remains in the tree.
    severity: medium
    detection_hint: >
      Any reintroduction of client-cert header parsing (X-Ssl-Client-*) or an ACME/mTLS
      trust path in provider.go or a new verify file should re-open this threat and
      verify the coordinator cannot be reached directly bypassing proxy header stripping.

  - id: T-023
    trust_boundary: TB-005
    stride: Denial_of_Service
    title: Apple MDA service unavailability degrades hardware trust verification
    description: >
      The MDA verification step depends on Apple's attestation servers responding
      to DevicePropertiesAttestation requests. Prolonged Apple-side unavailability
      prevents new providers from reaching hardware trust level.
    adversaries: []
    affected_assets: [A-010]
    affected_files:
      - coordinator/mdm/mdm.go
      - coordinator/attestation/**
    mitigations:
      - description: Providers can still operate at self_signed trust level without MDA.
        status: implemented
      - description: No timeout/circuit-breaker documented for MDA requests.
        status: open
    open_findings: []
    severity: low
    detection_hint: >
      Changes to MDM/MDA request handling should verify graceful degradation to
      self_signed trust rather than blocking provider registration indefinitely.

  # ── TB-006: Admin → Coordinator ──────────────────────────────

  - id: T-024
    trust_boundary: TB-006
    stride: Spoofing
    title: Admin API key compromise allows release and trust manipulation
    description: >
      The admin key controls release registration (binary hash registry), provider
      trust overrides, and platform configuration. Compromise via key leak, brute
      force, or non-constant-time comparison allows an attacker to register malicious
      binary hashes or deactivate legitimate releases.
    adversaries: [ADV-003]
    affected_assets: [A-005, A-011]
    affected_files:
      - coordinator/api/release_handlers.go
      - coordinator/api/server.go
    mitigations:
      - description: Release key gated by separate middleware from consumer keys.
        status: implemented
      - description: Release key uses plain string equality, not constant-time compare (SEC-009).
        status: open
      - description: No alerting or audit log on release registration events.
        status: open
    open_findings: [SEC-009]
    severity: critical
    detection_hint: >
      Any change to release key validation, admin middleware, or the /v1/releases
      handler must use constant-time key comparison and emit an audit log entry.

  - id: T-025
    trust_boundary: TB-006
    stride: Tampering
    title: Stale release cache serves revoked binary hashes after deletion
    description: >
      Cache invalidation on release registration uses the wrong key
      (latest_release:v1 instead of latest_release:v1:<platform>). After a release
      is deactivated, providers and installers may continue receiving the old
      release metadata from the per-platform cache.
    adversaries: [ADV-003]
    affected_assets: [A-011]
    affected_files:
      - coordinator/api/release_handlers.go
    mitigations:
      - description: Cache invalidation implemented but targeting wrong key (SEC-021).
        status: open
    open_findings: [SEC-021]
    severity: medium
    detection_hint: >
      Any change to release registration, deletion, or cache logic in
      release_handlers.go must verify that platform-specific cache keys are
      invalidated on both register and delete operations.

  - id: T-026
    trust_boundary: TB-006
    stride: Denial_of_Service
    title: Unauthenticated admin OTP endpoints enable spam and brute-force
    description: >
      Admin OTP init and verify endpoints are unauthenticated and not rate-limited.
      An attacker can spam OTP requests causing notification fatigue or attempt
      brute-force against Privy OTP controls.
    adversaries: [ADV-003]
    affected_assets: []
    affected_files:
      - coordinator/api/release_handlers.go
    mitigations:
      - description: No per-IP or per-email rate limit on OTP endpoints.
        status: open
    open_findings: [SEC-028]
    severity: low
    detection_hint: >
      Changes to admin OTP endpoint handlers should add rate limiting and
      generic error responses to prevent enumeration.

  # ── TB-007: Provider Inference Engine ────────────────────────

  - id: T-027
    trust_boundary: TB-007
    stride: Tampering
    title: Model weight substitution causes inference on unadvertised model
    description: >
      If the weight-hash check is fail-open (SEC-007), a provider serving from
      substituted weights will pass the routing gate. Consumers receive outputs
      from an unknown model while trusting the advertised model's properties.
    adversaries: [ADV-001]
    affected_assets: [A-004, A-001]
    affected_files:
      - provider-swift/Sources/ProviderCoreFoundation/WeightHasher.swift
      - provider-swift/Tests/ProviderCoreFoundationTests/WeightHasherMemoryTests.swift
      - coordinator/registry/registry.go
    mitigations:
      - description: Weight hash computed by WeightHasher and advertised in registration.
        status: implemented
      - description: Coordinator enforcement is fail-open when hash is omitted (SEC-007).
        status: open
    open_findings: [SEC-007]
    severity: high
    detection_hint: >
      This threat is a mirror of T-012 at the inference layer. Changes to weight
      hashing or coordinator enforcement should be reviewed together.

  - id: T-028
    trust_boundary: TB-007
    stride: Information_Disclosure
    title: Residual inference data in GPU memory accessible after request completes
    description: >
      Decrypted prompt tokens and KV-cache data remain in GPU memory until
      overwritten by the next request. A provider who can read GPU memory between
      requests could recover prior prompt content.
    adversaries: [ADV-001]
    affected_assets: [A-001]
    affected_files:
      - provider-swift/Sources/ProviderCore/Inference/EngineV2Bridge.swift
      - provider-swift/Sources/ProviderCore/Inference/EngineV2Runtime.swift
      - libs/mlx-swift-lm/Libraries/MLXLMCommon/ContinuousBatchingV2/SequenceKV/ContiguousKVBackend.swift
    mitigations:
      - description: CPU-side inference buffers are zeroed after each request.
        status: implemented
      - description: GPU-side MLX buffers (Metal) are not explicitly zeroed between requests.
        status: open
      - description: >
          No hardware memory isolation exists for inference memory. Hypervisor
          Stage 2 page-table isolation was never implemented; the
          hypervisor_active attestation field was a hardcoded-false stub and is
          retired from every trust decision. The coordinator still decodes it when
          a provider sends it (AttestationResponseMessage.HypervisorActive) and
          includes it in the status canonical only in that case
          (attestation.go BuildStatusCanonical), so legacy signed payloads keep
          verifying; current providers do not send it.
        status: open
    open_findings: []
    severity: medium
    detection_hint: >
      Changes to the inference pipeline, KV-cache management, or batch scheduling
      that affect buffer lifecycle should consider GPU-side residual data.

  - id: T-029
    trust_boundary: TB-007
    stride: Denial_of_Service
    title: Idle GPU timeout not triggered, monopolising provider resources
    description: >
      If IdleTimeoutPolicy fails to unload the model after inactivity, the GPU
      remains occupied and the provider cannot serve other models or recover memory.
    adversaries: [ADV-002]
    affected_assets: []
    affected_files:
      - provider-swift/Sources/ProviderCore/Inference/IdleTimeoutPolicy.swift
    mitigations:
      - description: IdleTimeoutPolicy unloads model after 1 hour of no requests.
        status: implemented
    open_findings: []
    severity: low
    detection_hint: >
      Changes to IdleTimeoutPolicy.swift or the inference loop that could defer
      or skip idle detection should be reviewed.

  - id: T-041
    trust_boundary: TB-007
    stride: Information_Disclosure
    title: Cross-tenant prefix-cache sharing + TTFT timing oracle (v0.7.5 ships the encrypted SSD offload tier — HMAC-keyed at-rest artifacts, default-on for CBv2-supported models with per-donation gating)
    description: >
      The encrypted prefix KV cache (DARKBLOOM_PREFIX_CACHE) is shared across all
      consumers on a provider — the provider cannot see tenant identity, so a KV
      prefix cached for one consumer is reused for any other consumer whose prompt
      shares that prefix. This creates two cross-tenant leaks that AT-REST
      encryption does NOT mitigate (it only defends disk theft / a foreign process,
      not co-tenants on the same live provider):
        1. TTFT timing oracle: the time-to-first-token difference between a cache
           HIT and a MISS lets a consumer who already knows (or guesses) an exact
           prompt prefix detect whether another consumer recently submitted it —
           i.e. confirm the presence of a known prefix in another tenant's traffic.
        2. Plaintext on-disk metadata (the GCM AAD, not encrypted) carries a STABLE
           tokenPrefixHash = SHA-256 of the prefix token IDs, plus tokenCount /
           model binding / timestamps. A disk observer (NOT a co-tenant — see T-028
           for the in-memory variant) can hash a guessed prefix and test it against
           on-disk filenames/metadata to confirm a known prefix was cached.
      As of 2026-06, the cache is ON BY DEFAULT (operator decision); it was
      previously opt-in. Default-on makes this channel the baseline exposure for
      every provider unless explicitly disabled.
      As of v0.7.5 the serving engine is CBv2 and the legacy engine/on-disk tier
      are deleted. The default successor SSD tier does write encrypted DBK3
      blocks, but closes leak #2 with KEK-derived HMAC lookup tags: raw prefix
      hashes and token IDs never touch disk. The plaintext tokenPrefixHash
      description above is therefore historical and does not describe KVCacheSSD.
      Exact routing closes the cross-account form of leak #1. The coordinator
      derives cacheSalt from authenticated account ID, concrete model build,
      prompt-contract ID, and block-hash version under a server HMAC key
      (coordinator/registry/cache_route_keys.go). Caller prompt_cache_key and
      user fields do not participate. Entries from different accounts therefore
      cannot resolve or be preferred across scopes. A same-account caller can
      still distinguish hit from miss by first-token latency; SEC-035 remains
      the accepted risk for that narrower trust boundary.
      CURRENT SHIP DECISION: encrypted SSD is the sole production tier and is
      default-on for every eligible CBv2 model, with a PER-DONATION
      gate (replacing the earlier per-model funding rule for this tier):
      a donation persists only when its whole-block prefix exceeds the
      model's adoption bound + the 1,024-token benefit floor. gpt-oss
      (bound 1,536) therefore skips never-adoptable sub-~2.5k donations
      (pure wear savings); gemma-4 (bound 25,600) caches exactly its
      >26.6k long-context tail — at-rest exposure UNCHANGED IN KIND
      (same HMAC names, same 15-min TTL, same crypto), merely extended to
      gemma's long-context traffic. SSD construction can be disabled by the
      single DARKBLOOM_PREFIX_CACHE kill switch or an unavailable KEK; when
      active, individual donations
      can still be declined by the adoption/benefit, staging, endurance, or
      disk guards. This review is the mandated
      sign-off for the tier's reintroduction of an at-rest artifact (the
      leak #2 class). Posture of the SSD tier
      (provider-swift/Sources/ProviderCore/KVCacheSSD/):
        (a) LEAK #2 CLOSED, not re-accepted: on-disk file names, the RAM
            index, and file metadata carry ONLY HMAC-SHA256 tags under
            K_lookup = HKDF(KEK, "dbkv3-lookup-v1") — a per-install secret
            rooted in the Secure Enclave via the existing KEK chain. Raw
            chain hashes / token-prefix hashes never touch disk, so a disk
            observer cannot test a guessed prefix against anything. The
            long-open HMAC mitigation ships IMPLEMENTED with the tier that
            re-opened it.
        (b) The at-rest window is bounded by a 15-MINUTE sliding TTL
            (DARKBLOOM_PREFIX_CACHE_SSD_TTL_SECONDS can only shorten it;
            sliding on hit; opportunistic sweep on write + periodic task);
            a 20 GiB box-wide LRU disk budget (DARKBLOOM_PREFIX_CACHE_DISK_GB,
            min'd with free-disk/2) unlinks oldest-by-last-hit.
        (c) File crypto is the reviewed legacy scheme unchanged: per-file
            random DEK wrapped AES-256-GCM under the SE-rooted KEK,
            HKDF-derived per-chunk nonces ("dbkv-chunk-v3"), canonical
            metadata JSON as AAD on every chunk (tamper ⇒ auth failure ⇒
            delete + recompute), atomic tmp+rename writes (DBK3 format,
            version 2).
        (d) Salt scoping is PRESERVED on disk: the request's cacheSalt is
            folded into both the engine chain hash AND (length-prefixed)
            the HMAC tag input, so entries donated under different scopes
            can never resolve — or even name-collide with — each other.
        (e) NO persistent KV memory carve: the engine keeps its full KV grant.
            Transient RAM includes read-through staging (one shared reservation
            per staged entry; requests attach) and bounded host Data in the
            write-behind donation pipeline. Refusal/drop preserves live serving
            and falls back to recompute.
      Residual at-rest metadata, accepted: file sizes (uniform block
      shape), file count, mtimes/createdAt (activity timing), and the
      cached-model identity (modelKey directory — already public via
      heartbeats). None of these confirm a specific prefix.
      Kill switch: DARKBLOOM_PREFIX_CACHE=0 disables the sole production
      tier, encrypted SSD. There is no production RAM tier or tier
      composition. Purge points: KEK wipe makes all files
      unreadable AND unfindable (K_lookup derives from the KEK).
      SEC-035 (leak #1, the in-process TTFT oracle) is UNCHANGED IN KIND
      and stays the accepted risk — with one extension for this sign-off:
      durability extends the oracle across restarts/idle-unloads within
      the 15-minute TTL window (a co-tenant can probe whether a prefix was
      cached earlier the same quarter-hour). Cache scope is coordinator-authored
      from authenticated account, model build, and prompt-contract identity;
      callers cannot choose or override it.
    adversaries: [ADV-002]
    affected_assets: [A-001]
    affected_files:
      # (BatchScheduler / PrefixCacheManager / EncryptedPrefixCachePersistence
      # were deleted with the legacy engine in v0.7.5; EncryptedKVStore is
      # PRESERVED as the chunked AES-GCM store the SSD-offload successor
      # reuses.)
      - provider-swift/Sources/ProviderCore/KVCache/EncryptedKVStore.swift
      - provider-swift/Sources/ProviderCore/KVCacheSSD/SSDPrefixCache.swift
      - provider-swift/Sources/ProviderCore/KVCacheSSD/SSDBlockStore.swift
      - provider-swift/Sources/ProviderCore/KVCacheSSD/SSDLookupKeys.swift
      - provider-swift/Sources/ProviderCore/KVCacheSSD/SSDBlockIndex.swift
      - provider-swift/Sources/ProviderCore/KVCacheSSD/SSDWriteBehind.swift
      - provider-swift/Sources/ProviderCore/KVCacheSSD/SSDPrefixCachePolicy.swift
      - provider-swift/Sources/ProviderCore/KVCacheSSD/SSDPrefixCacheFactory.swift
      - provider-swift/Sources/ProviderCore/Inference/PrefixCachePolicy.swift
      - provider-swift/Sources/ProviderCore/Inference/EngineV2SlotFactory.swift
      - provider-swift/Sources/ProviderCore/Inference/EngineV2Factory+Production.swift
      - provider-swift/Sources/ProviderCore/Inference/EngineV2Bridge.swift
      - provider-swift/Sources/ProviderCore/ProviderLoop+EngineV2.swift
    mitigations:
      - description: >
          AES-256-GCM encryption at rest (per-file DEK wrapped by an SE-derived KEK)
          — closes disk-theft + foreign-process reads, but NOT the cross-tenant
          sharing or TTFT timing channel.
        status: implemented
      - description: >
          DARKBLOOM_PREFIX_CACHE is the single production gate. Unset or an
          explicit affirmative value enables encrypted SSD; any explicit
          non-affirmative value disables it. Production never constructs or
          funds the upstream resident-RAM PrefixCacheV2 implementation
          (ProviderCore/Inference/PrefixCachePolicy.swift and
          EngineV2SlotFactory.swift).
        status: implemented
      - description: >
          Protocol-v2 cacheSalt is coordinator-authored from authenticated
          account, model build, prompt contract, and block-hash version under
          a server HMAC key. Entries from different accounts can never resolve.
          The SSD tier folds the opaque scope into both the canonical block
          chain and HMAC lookup tag, so files are unfindable and unusable
          outside that scope. Caller prompt_cache_key/user values are ignored.
        status: implemented
      - description: >
          SSD tier at-rest window bound: 15-minute sliding TTL (env can only
          shorten), 20 GiB box-wide LRU disk budget, verified weightHash in
          coordinator mode (model-ID fallback in standalone) + layoutEpoch
          fail-closed bindings, and KEK-wipe purge (files become unreadable AND
          unfindable — K_lookup derives from the KEK).
        status: implemented
      - description: >
          SSD tier NO-persistent-memory-carve posture: the engine keeps its full
          slot KV grant and production creates no resident-RAM prefix cache.
          Transient RAM includes read-through staging (one shared
          GlobalKVCacheBudget reservation per staged entry, with requests
          attaching) and bounded host Data in the write-behind donation
          pipeline. Refusal/drop preserves live serving and falls back to
          recompute.
        status: implemented
      - description: >
          ACCEPTED RISK (SEC-035): accepts the cross-tenant timing/sharing channel
          for deployments running a prefix cache, per an explicit operator
          decision. Requires a per-deployment threat-model sign-off before serving
          multi-tenant traffic with the cache on. Status is "open" because this is
          a documented accepted risk, not a code mitigation. Scope at v0.7.5:
          the SSD tier is DEFAULT-ON, so the acceptance again covers the fleet
          baseline (as it did under the 2026-06 default-on legacy posture) —
          but the covered channel is strictly narrower than legacy: leak #2 is
          CLOSED (HMAC-keyed names, vs legacy's plaintext tokenPrefixHash),
          the at-rest window is TTL/LRU-bounded, authenticated account scoping
          closes cross-account reuse, per-donation gating caches only adoptable
          long prefixes, and DARKBLOOM_PREFIX_CACHE=0 survives restarts. The
          residual same-account in-process TTFT oracle can extend across
          restarts within the 15-minute TTL, as accepted in the description.
        status: open
      - description: >
          Per-deployment gating by trust level (e.g. force-off for shared/untrusted
          multi-tenant, on only for single-tenant / hardware-trust) is not enforced
          in code — the operator must opt out manually.
        status: open
      - description: >
          HMAC the on-disk prefix identity with a per-install secret so a disk
          observer cannot recompute it from a guessed prefix (mitigates leak #2).
          IMPLEMENTED for the v2 SSD tier (v0.7.5): file names, index keys, and
          metadata carry only HMAC-SHA256 tags under K_lookup =
          HKDF(SE-rooted KEK, "dbkv3-lookup-v1"), with the request salt
          length-prefix-folded into the tag input (SSDLookupKeys). The v0.7.5
          startup sweep deletes the retired tier's old files, and the new tier
          never reads them.
        status: implemented
    open_findings: [SEC-035]
    severity: medium
    detection_hint: >
      A persistence tier now EXISTS (KVCacheSSD, v0.7.5). Review under this
      finding: any change to the production gate in
      PrefixCachePolicy (DARKBLOOM_PREFIX_CACHE semantics —
      the SSD tier is default-on with a PER-DONATION gate: adoption bound +
      benefit floor in SSDPrefixCache.donate; loosening it widens the
      at-rest surface), to the TTL or disk-budget
      policy (SSDPrefixCachePolicy.ttlSeconds 15-min max,
      PrefixCachePolicy.ssdDiskBudgetBytes), to the NAME DERIVATION
      (SSDLookupKeys: HKDF info string, HMAC domain tag, salt length-prefix,
      tag truncation), to the DBK3 metadata fields or AAD construction
      (SSDBlockStore — nothing prompt-derived beyond the keyed lookupTag may
      ever be added), to the purge points or the tier's own on-disk root
      (darkbloom/kv3 — deliberately OUTSIDE the legacy kv/ root that the
      upgrade sweeper sheds; KEK wipe remains the crypto purge), or to the
      staging-reservation release paths
      (SSDPrefixCache.endAdoption/completeStaging, EngineV2Bridge backstops).
      Also still: coordinator-authenticated scope derivation
      (coordinator/registry/cache_route_keys.go), cacheScope →
      CBv2Request.cacheSalt plumbing, and EngineV2Factory production SSD
      construction. Raising the TTL past 15 minutes or weakening authenticated
      account isolation needs a fresh SEC-035 sign-off.

  # ── TB-008: Coordinator → Payments ───────────────────────────

  - id: T-030
    trust_boundary: TB-008
    stride: Tampering
    title: Duplicate Stripe webhook credits balance twice
    description: >
      The Stripe checkout session completion and balance credit are not one atomic
      idempotent transaction. Concurrent duplicate valid webhooks can both pass the
      completed-session check before either marks the session complete, doubling the
      credit.
    adversaries: [ADV-002, ADV-003]
    affected_assets: [A-009]
    affected_files:
      - coordinator/api/billing_handlers.go
      - coordinator/store/postgres.go
    mitigations:
      - description: Completed-session check exists but is not atomic with credit insertion.
        status: open
    open_findings: [SEC-012]
    severity: high
    detection_hint: >
      Any change to Stripe webhook handling or balance credit logic must verify
      idempotency via a unique constraint on the Stripe session/event ID within
      the same database transaction.

  - id: T-031
    trust_boundary: TB-008
    stride: Information_Disclosure
    title: Provider earnings enumerable by wallet address without authentication
    description: >
      The legacy /v1/provider/earnings endpoint returns balance and payment history
      by wallet address with no authentication. Any party who knows or can guess
      a provider's wallet address can retrieve their earnings history.
    adversaries: [ADV-002, ADV-003]
    affected_assets: [A-009]
    affected_files:
      - coordinator/api/consumer.go
    mitigations:
      - description: Endpoint is unauthenticated by design for legacy reasons.
        status: open
    open_findings: [SEC-030]
    severity: low
    detection_hint: >
      Changes to earnings or balance endpoints should require account authentication
      unless public transparency is an explicit product decision.

  - id: T-032
    trust_boundary: TB-001
    stride: Spoofing
    title: Coordinator encryption mnemonic exfiltration from environment
    description: >
      The coordinator's BIP39 mnemonic derives its X25519 request-decryption
      identity. A compromised coordinator process, environment file, or host
      access policy would allow an attacker to derive that private key and break
      sender-to-coordinator sealed-request confidentiality.
    adversaries: [ADV-003]
    affected_assets: [A-006]
    affected_files:
      - coordinator/billing/**
      - coordinator/internal/e2e/**
      - coordinator/cmd/coordinator/main.go
    mitigations:
      - description: Mnemonic is injected through the root-only production environment.
        status: implemented
      - description: Coordinator runs in a GCP AMD SEV Confidential VM; memory is encrypted by hardware.
        status: implemented
    open_findings: []
    severity: critical
    detection_hint: >
      Any change that reads, logs, or transmits the mnemonic or derived X25519
      private key outside the key-derivation path is critical.

  # ── TB-009: Apple Attestation Trust Chain (cross-cutting) ─────

  - id: T-033
    trust_boundary: TB-009
    stride: Spoofing
    title: Attestation blob replay from a previously trusted provider
    description: >
      An attacker captures a valid attestation blob from a trusted provider and
      replays it from a different machine to inherit that provider's trust level.
    adversaries: [ADV-001, ADV-003]
    affected_assets: [A-010]
    affected_files:
      - coordinator/attestation/**
      - coordinator/api/provider.go
      - provider-swift/Sources/ProviderCore/Security/AttestationBuilder.swift
      - provider-swift/Sources/ProviderCore/Security/PersistentEnclaveKey.swift
      - provider-swift/Sources/ProviderCore/Security/AttestationSigner.swift
    mitigations:
      - description: Attestation blob includes ISO 8601 timestamp; coordinator checks freshness.
        status: implemented
      - description: 5-minute challenge-response requires signing a fresh coordinator-generated nonce with the SE key.
        status: implemented
      - description: SE key is hardware-bound and persistent; replay without the originating SE is cryptographically infeasible.
        status: implemented
    open_findings: []
    severity: critical
    detection_hint: >
      Changes to attestation blob construction, timestamp validation, the
      challenge-response nonce flow, or the PersistentEnclaveKey / AttestationSigner
      protocol must preserve replay protection.

  - id: T-034
    trust_boundary: TB-009
    stride: Tampering
    title: Provider runs modified code while advertising a trusted identity
    description: >
      The original control was a SHA-256 binary hash in the attestation blob. That
      hash is SELF-REPORTED by the provider's own (possibly modified) code, so it is
      worthless against a malicious operator: a fork can report a blessed hash while
      running prompt-logging code. The real control is APNs code-identity attestation
      (v0.6.0): only our genuine, Team-ID-signed, push-provisioned binary can RECEIVE
      an APNs push for our App-ID, and only the genuine hardened process can DECRYPT
      the E_K(nonce) challenge with its in-memory key K and sign the nonce with its
      registration-bound SE key — re-proved on every WebSocket connection, either by
      a fresh APNs push or, when a durable proof for the same SE key + APNs token
      younger than the 30-minute reuseWindow exists, by a NaCl-sealed resume challenge
      over the WebSocket (code_attestation_resume_challenge, 30 s) that costs no APNs
      budget. Pushes are throttled (20 min background / 75 s alert cooldown per device,
      3 attempts per loop, 300 s challenge validity; code_attest_throttle.go).
    adversaries: [ADV-001]
    affected_assets: [A-010, A-011]
    affected_files:
      - coordinator/apns/attestor.go
      - coordinator/api/provider.go
      - coordinator/registry/registry.go
      - provider-swift/Sources/ProviderCore/ProviderLoop.swift
      - provider-swift/Sources/darkbloom/ProviderAppKitHost.swift
    mitigations:
      - description: >
          APNs code-identity round-trip: coordinator pushes E_K(nonce) to the
          registration-bound device token; provider decrypts with K and replies
          nonce + Sign_SE(nonce) over the WebSocket; coordinator verifies the nonce
          matches and the SE signature verifies against the registration-bound key.
        status: implemented
      - description: >
          CodeAttested gate at the single routing chokepoint
          (providerSupportsPrivateTextLocked), fail-closed, no self-route exemption;
          enforced once an attestor is configured AND the APNS_ENFORCE_AFTER deadline
          has passed (codeAttestationEnforcedLocked); before that providers are
          challenged and coverage is measured but nobody is derouted.
        status: implemented
      - description: >
          Self-reported binaryHash DEMOTED to drift telemetry — derouting on mismatch
          is behind a default-off binaryHashEnforce flag. It is no longer relied on as
          a code-identity control.
        status: implemented
      - description: >
          Strongest version (cross-cutting): reproducible build + public transparency
          log of blessed cdhashes, so the genuine APNs-proven binary's self-reported
          cdhash becomes auditable (version/downgrade control).
        status: open
    open_findings: []
    severity: critical
    detection_hint: >
      Changes to coordinator/apns, the per-connection push-attest step in
      api/provider.go, the CodeAttested gate in registry, or the provider's
      decrypt→Sign_SE→reply handler must preserve the property that only genuine,
      Apple-provisioned code can pass. Re-relying on binaryHash as a security control
      (vs telemetry) is a regression.

  - id: T-035
    trust_boundary: TB-009
    stride: Repudiation
    title: Provider denies performing actions after key rotation
    description: >
      The SE key is now persistent across process restarts (keychain-backed, label
      io.darkbloom.provider.attestation-signing.v2). Key rotation occurs only on
      explicit keychain deletion (factory reset, device wipe, or deliberate operator
      action). After rotation the new key is unlinked from prior attestations; audit
      trails tied to the old public key cannot be attributed to the new identity. A
      provider could opportunistically delete the keychain item to discard the audit
      chain tied to a prior identity.
    adversaries: [ADV-001]
    affected_assets: [A-002, A-010]
    affected_files:
      - provider-swift/Sources/ProviderCore/Security/PersistentEnclaveKey.swift
      - provider-swift/Sources/ProviderCore/Security/SecureEnclaveIdentity.swift
      - coordinator/api/provider.go
    mitigations:
      - description: Coordinator logs registration events with SE public key fingerprint.
        status: implemented
      - description: Persistent key (kSecAttrIsPermanent) survives process restarts, reducing
          involuntary rotation events compared to the prior ephemeral design.
        status: implemented
      - description: No explicit key-rotation policy, cross-signing between old and new SE keys,
          or coordinator-side detection when a provider re-registers with a new key.
        status: open
    open_findings: []
    severity: medium
    detection_hint: >
      Changes to PersistentEnclaveKey.swift key creation/deletion paths, or provider
      re-enrollment flows, should consider whether key rotation is logged and whether
      prior audit trails remain valid. A provider that re-registers with a new SE key
      after an unexplained keychain delete should be flagged.

  - id: T-036
    trust_boundary: TB-009
    stride: Elevation_of_Privilege
    title: Trust level elevated without completing full MDM/MDA chain
    description: >
      If any step in the hardware-trust chain (SE signature check, MDM SecurityInfo
      callback) is bypassed or short-circuited — whether by a code bug, a test flag
      left in production, or an MDM webhook deployed without its shared secret
      (SEC-004) — a provider can reach hardware trust level without Apple's genuine
      verification. The MDA certificate chain is verified afterwards and surfaced as
      the separate mda_verified flag; it neither grants nor blocks the level.
    adversaries: [ADV-001, ADV-003]
    affected_assets: [A-010]
    affected_files:
      - coordinator/attestation/**
      - coordinator/mdm/mdm.go
      - coordinator/api/provider.go
      - coordinator/registry/registry.go
    mitigations:
      - description: >
          Hardware grant requires SE signature verification and then an MDM SecurityInfo
          cross-check whose SIP and SecureBootLevel == full agree with the SE blob
          (verifyProviderViaMDM); a posture mismatch proven by a received SecurityInfo
          is terminal (MarkUntrusted), while lookup failures and timeouts leave trust
          unchanged and retry. The MDA cert chain from the Apple root CA is verified
          afterwards and attached only if hardware is already held
          (SetMDAProofIfHardwareBound). A stored hardware level is never restored on
          reconnect (RestoreProviderState caps to self_signed).
        status: implemented
      - description: MDM webhook shared secret is optional; where unset, an injected callback matching a live CommandUUID can manipulate SecurityInfo state (SEC-004).
        status: open
      - description: Attestation tests require AuthenticatedRootEnabled=true or earlier errors are overwritten.
        status: implemented
      - description: >
          DAR-326 trust-reuse fast-skip (coordinator/api/trust_reuse.go tryTrustReuseFastSkip)
          lets a reconnecting, recently-fully-verified provider regain hardware trust from a
          persisted record without re-running the live MDM SecurityInfo round-trip — avoiding a
          fleet-wide MDM/APNs re-verification herd on a planned coordinator restart/swap. ACCEPTED
          TRADE-OFF (relative to TB-005): within the reuse admission the fast-skip captures the
          prior SIP/Secure-Boot posture but does NOT re-prove MDA cert-chain freshness. Admission
          requires ONE of two premises: (a) STALENESS BOUND — the last live MDM verification is
          within the reuse window (EIGENINFERENCE_TRUST_REUSE_WINDOW, default 5m, tightened from
          10m — cannot span a SIP-disable reboot cycle); or (b) CONNECTION CONTINUITY — the
          provider was live-verified, stayed continuously connected and hardware-trusted (the
          coordinator batch-advances a durable continuous_coverage_until watermark ~every 30s
          while it observes the live SE-challenged connection, plus exact-time disconnect and
          graceful-shutdown sweeps), and the coordinator-MEASURED offline gap
          (now - continuous_coverage_until) is within the reconnect allowance
          (EIGENINFERENCE_TRUST_REUSE_RECONNECT_GAP, default 90s, HARD-clamped to [0,120s] —
          values above the ceiling clamp DOWN). Premise (b) is sound on RecoveryOS physics:
          SIP/Secure Boot can only change in RecoveryOS, and entering and leaving Recovery on
          Apple Silicon (One True Recovery: manual power-button entry, credentialed
          csrutil/bputil, two boot transitions) takes >= ~3 minutes and drops any WebSocket, so a
          contiguous coordinator-measured gap <= 120s cannot span a posture flip. The gap is
          never provider-claimed; a coordinator crash leaves the last periodic watermark standing
          so the gap is only ever OVER-estimated (fail-safe). Chained sub-allowance gaps are safe:
          each continuity reconnect runs the live SE challenge (proving a normal-OS boot) and
          re-anchors coverage at the grant, while the hardware-proof timestamp is NEVER advanced
          by reuse. Both premises stay bounded by (1) the ALWAYS-run live SE challenge that
          re-proves identity + SIP/Secure-Boot posture + approved binary on every reuse, (2) a
          durable hard-untrust epoch/tombstone that refuses/removes a record if a hard untrust
          races the grant (tombstones win; coverage never resurrects), and (3) declining the
          fast-skip entirely when no MDM client is configured. A genuine MDA re-verification
          still runs for every first-ever connection and for any reconnect failing both premises
          or any other gate. Continuity grants carry a distinct decision label ("continuity" /
          "continuity_release_transition") in logs and metrics so both premises are observable.
        status: accepted
    open_findings: [SEC-004]
    severity: critical
    detection_hint: >
      Any change to the attestation verification sequence, trust-level assignment
      logic, or MDM webhook handling is critical. Verify each step in the chain is
      required and cannot be individually bypassed.

  - id: T-037
    trust_boundary: TB-009
    stride: Information_Disclosure
    title: SIP/SecureBoot state self-reported without independent verification between challenges
    description: >
      Between 5-minute challenge intervals, the coordinator relies on the provider's
      self-reported SIP and SecureBoot state. A provider could report true at the
      challenge boundary and then disable protections until the next challenge.
    adversaries: [ADV-001]
    affected_assets: [A-001, A-010]
    affected_files:
      - provider-swift/Sources/ProviderCore/Security/SecurityHardening.swift
      - coordinator/api/provider.go
    mitigations:
      - description: SIP checked at startup, before every request, and in every 5-minute challenge.
        status: implemented
      - description: Disabling SIP requires reboot (kills process); practical attack window is extremely narrow.
        status: implemented
      - description: MDM SecurityInfo provides independent SIP verification on demand.
        status: implemented
    open_findings: []
    severity: medium
    detection_hint: >
      Changes to challenge cadence, SIP-check frequency, or the per-request SIP
      gate in SecurityHardening.swift should preserve defense-in-depth against
      this narrow window.

  # ── HTTP Server Hardening (coordinator-wide) ──────────────────

  - id: T-038
    trust_boundary: TB-001
    stride: Denial_of_Service
    title: Missing ReadHeaderTimeout and MaxHeaderBytes enable slow-header attacks
    description: >
      The coordinator HTTP server sets ReadTimeout and IdleTimeout but omits
      ReadHeaderTimeout and MaxHeaderBytes. Slow-header attacks or clients sending
      very large headers are less constrained at the application layer.
    adversaries: [ADV-002, ADV-003]
    affected_assets: []
    affected_files:
      - coordinator/cmd/coordinator/main.go
    mitigations:
      - description: ReadTimeout and IdleTimeout are set.
        status: implemented
      - description: >
          ReadHeaderTimeout (5s) and MaxHeaderBytes (64 KB) are set in
          cmd/coordinator/main.go, bounding the slow-header window and
          per-connection header memory.
        status: implemented
    open_findings: []
    severity: medium
    detection_hint: >
      Changes to the http.Server configuration in main.go should verify
      ReadHeaderTimeout and MaxHeaderBytes are set.

  - id: T-039
    trust_boundary: TB-002
    stride: Spoofing
    title: Device codes can mint multiple provider tokens before expiry
    description: >
      Approved device codes (RFC 8628 device auth flow) are not consumed atomically
      on first token exchange. Any party who obtains the device code before expiry
      can exchange it multiple times, minting multiple long-lived provider tokens.
    adversaries: [ADV-001, ADV-003]
    affected_assets: [A-008]
    affected_files:
      - coordinator/api/device_auth.go
      - coordinator/store/postgres.go
    mitigations:
      - description: Device codes have a validity window.
        status: implemented
      - description: Codes not consumed atomically on first exchange (SEC-011).
        status: open
    open_findings: [SEC-011]
    severity: medium
    detection_hint: >
      Changes to device_auth.go token exchange logic must verify the code is
      marked consumed in the same transaction as token issuance.

  - id: T-040
    trust_boundary: TB-001
    stride: Information_Disclosure
    title: Host header injection influences generated installer and enrollment URLs
    description: >
      Public URL generation falls back to the request Host and X-Forwarded-Proto
      headers when the baseURL config is unset. An attacker who can influence these
      headers can redirect install.sh downloads or device-link URLs to a
      controlled origin.
    adversaries: [ADV-003]
    affected_assets: [A-011]
    affected_files:
      - coordinator/api/server.go
      - coordinator/api/enroll.go
      - coordinator/api/consumer.go
    mitigations:
      - description: baseURL should be set in production; fallback to Host header is the risk.
        status: open
      - description: >
          enroll.go now generates SCEP/MDM URLs (the ACME payload was removed
          2026-07-03) via the shared resolveBaseURL
          (configured EIGENINFERENCE_BASE_URL), not a separate Host-derived value, so
          when baseURL is set the Host-header fallback is unreachable for the profile.
          This matters more now that the profile is CMS-signed (a spoofed Host would
          otherwise yield a Darkbloom-signed profile pointing enrollment at an
          attacker host).
        status: implemented
    open_findings: [SEC-013]
    severity: medium
    detection_hint: >
      Changes to URL generation in server.go, enroll.go, or consumer.go should
      verify that production baseURL is required and the Host-header fallback
      cannot be reached in production deployments.

  # ── TB-006: Profile-signing credential (added with CMS profile signing) ──

  - id: T-041
    trust_boundary: TB-006
    stride: Spoofing
    title: Configuration-profile signing identity exfiltration or misuse
    description: >
      The Developer ID Application signing identity is injected into the coordinator
      environment as PROFILE_SIGNING_P12_B64 (+ PROFILE_SIGNING_P12_PASSWORD) and used
      to CMS-sign the /v1/enroll .mobileconfig. A compromised coordinator process,
      misconfigured KMS policy, or environment-variable leak would let an attacker
      sign arbitrary profiles (or binaries) that macOS treats as coming from the
      registered developer — a higher-value code-signing trust abuse than payment
      secrets. Same injection pattern and risk class as the encryption mnemonic (T-032).
    adversaries: [ADV-003]
    affected_assets: [A-013]
    affected_files:
      - coordinator/profilesign/signer.go
      - coordinator/cmd/coordinator/main.go
      - coordinator/api/enroll.go
    mitigations:
      - description: Identity stored in KMS, injected at runtime (same pipeline as MDM_PUSH_P12_B64).
        status: implemented
      - description: Coordinator runs in a GCP AMD SEV Confidential VM; memory is encrypted by hardware.
        status: implemented
      - description: >
          LoadFromEnv never logs the p12 or password (only signer CN/org/expiry), and
          fails closed to unsigned on a missing/invalid/expired identity rather than
          stamping an untrusted signature.
        status: implemented
      - description: Zero the decoded p12/password from memory after key extraction.
        status: open
    open_findings: []
    severity: high
    detection_hint: >
      Any change in coordinator/profilesign or main.go that logs, transmits, or
      persists PROFILE_SIGNING_P12_B64 / _PASSWORD or the decoded key outside the
      signer must be treated as a high-severity issue.

  # ── TB-009: APNs code-identity mechanism's own attack surface ──

  - id: T-042
    trust_boundary: TB-009
    stride: Spoofing
    title: Forged code-identity via attacker-key challenge + log/disk harvest (R-snoop)
    description: >
      The APNs challenge (T-034) is encrypted to the provider's self-reported key K.
      A fork that registers its OWN key K' could, in principle, harvest the pushed
      E_K'(nonce) from a root-readable surface on its own machine and decrypt it with
      K' — passing attestation without being genuine code. This requires the push
      payload to be readable by root AND a genuine App-ID device token bound to K'.
      Residual: a determined attacker on their own hardware could manually enable
      private-data logging via the GUI AND harvest a genuine token AND race the
      genuine T↔K registration — the "patient insider on own hardware" class
      (consistent with T-037 and the irreducible residuals), not a casual log read.
    adversaries: [ADV-001]
    affected_assets: [A-001, A-010]
    affected_files:
      - coordinator/apns/attestor.go
      - provider-swift/Sources/darkbloom/ProviderAppKitHost.swift
      - provider-swift/Sources/ProviderCore/Apns/APNsBridge.swift
    mitigations:
      - description: >
          R-snoop verified CLOSED on current macOS (26.4, on-box): the encrypted
          code_challenge is redacted (<private>) in the unified log and absent from
          every on-disk store (apsd store, Notification Center DB) for background AND
          alert pushes; CLI un-redaction (log config private_data:on / profiles
          install) is removed. See docs/apns-code-attestation-design.md §1.6.1.
        status: implemented
      - description: >
          INVARIANT — the provider requests no UNUserNotificationCenter authorization,
          so an alert-mode push is never presented/persisted to the root-readable
          Notification Center DB. Enforced by code review + comments at both ends
          (ProviderAppKitHost.swift, coordinator/apns/attestor.go).
        status: implemented
      - description: >
          Challenge encrypted as E_K(nonce) (defense-in-depth for older macOS / a
          user-enabled private-data logging profile); the SE signature is verified
          against the key bound at registration, never a key supplied in the response.
        status: implemented
    open_findings: []
    severity: high
    detection_hint: >
      Reopens if any change (a) makes the provider request user-notification
      authorization, (b) carries code_challenge in an alert-rendering payload, or
      (c) logs the push payload. The closed-verification scope is current macOS
      (min target is 15); re-verify on a version bump.

  # ── TB-003: watchdog-owned update / rollback recovery authority ──

  - id: T-043
    trust_boundary: TB-003
    stride: Denial of Service
    title: Watchdog rollback authority is the same replaceable provider binary
    description: >
      The crash-recovery watchdog (`io.darkbloom.watchdog`) and the automatic
      predecessor rollback it performs run the SAME installed `darkbloom`
      binary that updates replace. This is a deliberate, documented limitation
      set, not an oversight. (1) Rollback protection depends on the pre-update
      watchdog PROCESS surviving a candidate's stabilization window: launchd
      KeepAlive keeps the old process alive across the install, but if that
      process dies (reboot, logout, crash) launchd relaunches the watchdog
      FROM the candidate binary. (2) Rollback therefore does NOT survive a
      reboot into a candidate broken at the binary level (dyld failure,
      code-signature rejection, eager crash before command dispatch) — such a
      host stays down until a manual reinstall. (3) The failed-version
      quarantine is intentionally single-slot and exact-version:
      quarantining v3 OVERWRITES the v2 quarantine record, and only normal
      monotonic version ordering (a quarantined version is never strictly
      newer than the rolled-back-to predecessor's successor) prevents v2 from
      reinstalling. No component may claim multi-version quarantine.
      (4) Rollback/replay verification is fail-closed on the pinned Darkbloom
      designated requirement (Team SLDQ2GJ6TL): legacy flat/ad-hoc or locally
      re-signed installs are NOT rollback-eligible; the watchdog logs an
      actionable refusal and preserves the live install, and the remedy is a
      signed reinstall via install.sh. This is an accepted fleet-impact
      decision — hosts on unsigned installs keep restart recovery but not
      rollback.
    adversaries: [ADV-001]
    affected_assets: [A-010]
    affected_files:
      - provider-swift/Sources/ProviderCore/Service/WatchdogAgent.swift
      - provider-swift/Sources/ProviderCore/Service/WatchdogRecoveryService.swift
      - provider-swift/Sources/ProviderCore/Update/UpdateRecoveryStore.swift
      - provider-swift/Sources/ProviderCore/Update/UpdateInstallLayout.swift
      - provider-swift/Sources/ProviderCore/Update/DarkbloomCodeSignature.swift
    mitigations:
      - description: >
          Persistent KeepAlive watchdog process with an internal monotonic
          scheduler (GUI StartInterval one-shots are stranded by launchd
          on-demand-only mode), so the pre-update process normally survives
          the whole stabilization window without respawning.
        status: implemented
      - description: >
          Journaled, fsync-fail-closed install/rollback transactions with
          crash replay, so every power-loss boundary converges to a fully
          verified install; predecessor material is hash+signature verified
          against the pinned designated requirement before any restore.
        status: implemented
      - description: >
          Bounded watchdog network I/O (URLSession request/resource timeouts)
          plus a safe-point per-tick budget so a stalled download or wedged
          tick cannot hold the cross-process update lock forever; the budget
          is only consulted between complete operations and can never
          interrupt a journaled commit.
        status: implemented
      - description: >
          A separately installed, independently signed minimal bootstrap
          executable outside the replaceable install tree, giving recovery
          authority that survives reboot into a binary-level-broken candidate.
        status: open
    open_findings: []
    severity: medium
    detection_hint: >
      Reopens if any code or docs claim rollback survives a reboot into a
      non-launchable candidate, claim multi-version quarantine, weaken the
      designated-requirement pin to structural codesign validity, or move the
      candidate-hung timeout below the operator's configured
      startup_preload_timeout_secs plus margin.

  # ── TB-010: Provider → privileged fan helper ─────────────────

  - id: T-044
    trust_boundary: TB-010
    stride: Elevation_of_Privilege
    title: Untrusted local process impersonates the provider to the root fan helper
    description: >
      A local process that can acquire or renew a helper lease can cause root-owned
      AppleSMC writes whenever the configured temperature threshold is met.
    adversaries: [ADV-001]
    affected_assets: [A-014]
    affected_files:
      - provider-swift/Sources/DarkbloomFanService/FanPeerAuthentication.swift
      - provider-swift/Sources/DarkbloomFanHelper/FanXPCService.swift
      - provider-swift/Sources/darkbloom/FanActivityLease.swift
    mitigations:
      - description: >
          Production XPC connections mutually pin exact signing identifiers and
          Developer ID Team SLDQ2GJ6TL using NSXPCConnection requirements; the
          helper separately restricts peers to root or the configured numeric UID
          whose directory-service GeneratedUID still matches the opt-in account.
        status: implemented
      - description: >
          The exported protocol contains only lease, status, and root-only Auto
          restoration operations; no arbitrary SMC write primitive exists.
        status: implemented
    open_findings: []
    severity: high
    detection_hint: >
      Reopens if production accepts identifier-only/ad-hoc peers, trusts a PID or
      user-controlled name instead of XPC credentials, or adds a generic SMC method.

  - id: T-045
    trust_boundary: TB-010
    stride: Tampering
    title: User-writable helper, plist, or policy turns launchd into root execution
    description: >
      A system LaunchDaemon that executes a binary or parses executable paths from a
      provider-owned home directory would convert ordinary account access into root.
    adversaries: [ADV-001]
    affected_assets: [A-014]
    affected_files:
      - provider-swift/Sources/darkbloom/Fan/FanServiceManager.swift
      - provider-swift/Sources/DarkbloomFanService/FanDurableFile.swift
      - provider-swift/Sources/ProviderCore/Update/FanHelperCapabilityVerifier.swift
    mitigations:
      - description: >
          The helper is copied to a fixed root:wheel 0755 path after exact signature
          verification; plist and policy are fixed-path, root-owned, non-symlink files.
        status: implemented
      - description: >
          Writes use same-directory temporary files, fchmod/fchown, fsync, atomic
          rename, and parent-directory fsync. The helper rejects unsafe file metadata.
        status: implemented
    open_findings: []
    severity: critical
    detection_hint: >
      Reopens if ProgramArguments points into ~/.darkbloom, a user supplies a path,
      signature/ownership checks weaken, or config grows command/path fields.

  - id: T-046
    trust_boundary: TB-010
    stride: Denial_of_Service
    title: Helper crash or partial SMC write strands fans in manual mode
    description: >
      Manual mode and Ftst can outlive the process that asserted them. A failure
      between multi-fan writes can also leave inconsistent fan state.
    adversaries: [ADV-001]
    affected_assets: [A-014]
    affected_files:
      - provider-swift/Sources/DarkbloomFanCore/FanController.swift
      - provider-swift/Sources/DarkbloomFanHelper/FanDaemon.swift
      - provider-swift/Sources/DarkbloomFanService/FanOwnershipRecovery.swift
    mitigations:
      - description: >
          Controller writes are verified and transactional; any partial failure
          restores every possibly touched fan and clears only possibly owned Ftst.
        status: implemented
      - description: >
          A durable ownership journal is committed before the first possible SMC
          mutation; KeepAlive restart reconciles it before XPC activation.
        status: implemented
      - description: >
          Signal shutdown, sleep, administrator disable, and uninstall restore and
          verify Auto. Uninstall refuses to delete recovery state if restoration fails.
        status: implemented
    open_findings: []
    severity: high
    detection_hint: >
      Reopens if journal persistence moves after an SMC write, rollback stops after
      the first fan error, or shutdown/uninstall deletes an unresolved journal.

  - id: T-047
    trust_boundary: TB-010
    stride: Tampering
    title: Stale provider or downgrade keeps privileged fan control active
    description: >
      Provider stop, schedule closure, update, rollback, or a hung process must not
      leave a permanent root-authorized manual policy.
    adversaries: [ADV-001]
    affected_assets: [A-014]
    affected_files:
      - provider-swift/Sources/darkbloom/FanActivityLease.swift
      - provider-swift/Sources/darkbloom/StartCommand+Modes.swift
      - provider-swift/Sources/DarkbloomFanHelper/FanDaemon.swift
    mitigations:
      - description: >
          Connection-scoped leases renew every five seconds and expire after fifteen;
          invalidation and task cancellation request immediate restoration.
        status: implemented
      - description: >
          Sleep revokes the lease and wake requires a fresh renewal. Protocol mismatch
          and rollback to pre-lease versions leave the helper dormant in Auto.
        status: implemented
    open_findings: []
    severity: medium
    detection_hint: >
      Reopens if a lease becomes persistent, cancellation stops releasing it, or an
      incompatible provider can trigger manual mode.

  - id: T-048
    trust_boundary: TB-010
    stride: Denial_of_Service
    title: Incorrect private GPU sensor mapping applies an unsafe fan policy
    description: >
      Apple changes private SMC sensor names by chip family. A wrong or stale mapping
      can engage too early, fail to cool, or hold a manual ceiling while another
      component overheats.
    adversaries: []
    affected_assets: [A-014]
    affected_files:
      - provider-swift/Sources/DarkbloomFanCore/FanHardware.swift
      - provider-swift/Sources/DarkbloomFanCore/FanPolicy.swift
      - provider-swift/Sources/DarkbloomFanHelper/FanDaemon.swift
    mitigations:
      - description: >
          Explicit M1-M5 allowlists, runtime key/type probing, finite plausible-value
          checks, missing-sensor fail-safe, and read-only diagnose output.
        status: implemented
      - description: >
          Takeover never lowers the existing target; serious/critical macOS thermal
          pressure, invalid readings, or control failures restore system Auto.
        status: implemented
    open_findings: []
    severity: medium
    detection_hint: >
      Every new chip/model or macOS release needs GPU-load correlation and full
      engage/release/restore hardware validation before support is claimed.

  # ── TB-001: Coordinator-side remote media fetching (mediafetch) ──

  - id: T-049
    trust_boundary: TB-001
    stride: Spoofing
    title: SSRF / resource abuse via consumer-supplied remote media URLs
    description: >
      The coordinator fetches remote http(s) image_url/video_url links from chat
      completion requests and inlines them as base64 data: URIs before sealing the
      body to a provider (coordinator/mediafetch, api/media_resolve.go; default ON,
      EIGENINFERENCE_MEDIA_FETCH_ENABLED kill switch). This makes the coordinator —
      the TEE control plane — issue attacker-chosen outbound HTTP requests: the
      classic SSRF surface (internal services, cloud metadata, DNS rebinding,
      redirect laundering), plus resource abuse (decompression/pixel bombs, slow
      origins, fetch storms) and a privacy side channel (the URL's origin observes
      coordinator egress correlated with a request). The provider-side data:-only
      guard is unchanged, so providers never fetch anything (their SSRF posture is
      not affected).
    adversaries: [ADV-002, ADV-003]
    affected_assets: [A-001]
    affected_files:
      - coordinator/mediafetch/ssrf.go
      - coordinator/mediafetch/fetch.go
      - coordinator/mediafetch/budget.go
      - coordinator/mediafetch/sniff.go
      - coordinator/mediafetch/resolver.go
      - coordinator/mediafetch/config.go
      - coordinator/api/media_resolve.go
      - coordinator/api/consumer.go
      - coordinator/api/inference_admission.go
    mitigations:
      - description: >
          Connect-time IP deny policy in net.Dialer.Control — runs on the
          post-DNS-resolution address of EVERY dial including each redirect hop
          (DNS-rebinding/TOCTOU-proof). The IPv6 scope zone is stripped before the
          policy scan because netip.Prefix.Contains returns false for ANY zoned
          address, which would otherwise let a "%zone" suffix skip the whole
          prefix deny list. Denies loopback, RFC1918, link-local (incl.
          169.254.169.254 cloud metadata), CGNAT, "this network" 0.0.0.0/8,
          IPv4-mapped IPv6, and IPv6 transition/compatibility spaces that can
          embed/tunnel denied IPv4 destinations (IPv4-compatible ::/96, NAT64,
          6to4, Teredo, deprecated 6to4 relay anycast), plus deprecated
          site-local, multicast, unspecified, benchmarking/documentation/reserved
          ranges, and anything not global-unicast (mediafetch/ssrf.go ipAllowed,
          fail-closed).
        status: implemented
      - description: >
          URL policy: http/https schemes only, standard web ports only (HTTP 80,
          HTTPS 443; non-standard ports require the explicit dev/test override
          EIGENINFERENCE_MEDIA_FETCH_ALLOW_NONSTANDARD_PORTS), no embedded
          credentials, redirect depth capped at 3 with per-hop re-validation and
          the auto-populated Referer stripped on every hop (a presigned origin URL
          in the query string never leaks to a redirect target), optional operator
          domain blocklist that applies at DNS-label boundaries to each listed
          domain and all of its subdomains
          (EIGENINFERENCE_MEDIA_FETCH_BLOCKLIST_DOMAINS). Request hosts and
          blocklist entries are canonicalized identically (canonicalHost:
          lowercase, UTS-46 IDNA to ASCII, leading/trailing dots trimmed) so the
          set the policy matches is the set net/http actually dials — a Unicode
          separator homoglyph (U+3002/U+FF0E/U+FF61) cannot present one host to
          the blocklist and another to the transport, and the conventional
          wildcard spelling ".evil.com" cannot store a key that never matches.
        status: implemented
      - description: >
          Content policy: data: URI built from magic-byte sniffing (never the
          Content-Type header); explicit allowlist of provider-decodable formats
          (JPEG/PNG/GIF/WebP/BMP; MP4/QuickTime) — HTML/SVG/executables/WebM fail
          closed; declared part kind must match sniffed kind (image_url must be an
          image); header-only megapixel parsing covers every accepted image format
          (stdlib DecodeConfig for JPEG/PNG/GIF, explicit VP8/VP8L/VP8X and BMP DIB
          parsing for WebP/BMP) and mirrors the provider's pre-raster pixel gate.
          Provider decode-time caps (pixels, bytes, duration, frames) remain the
          authoritative second layer.
        status: implemented
      - description: >
          Resource bounds: per-file (8 MiB) cap via io.LimitReader plus one SHARED
          per-request byte budget enforced during every concurrent read (10 MiB +
          one overflow-sentinel byte, rather than checking only after each response
          returns). Duplicate canonical URLs are fetched and byte-accounted once
          and rewritten at every request location, so the raw byte caps do NOT see
          the per-location base64 multiplication: the projected inlined total
          (sum over targets of the data: URI length, saturating arithmetic) is
          therefore checked against MaxInlinedBytes (16 MiB, mirroring
          api.maxInferenceBodyBytes) BEFORE the request map is mutated, so eight
          parts sharing one 8 MiB URL are rejected instead of materializing an
          ~85 MiB marshal buffer. Content-Length is only a pre-check. No
          transparent decompression (gzip bombs), per-request worker cap (4),
          process-wide fetch semaphore (32), per-fetch (15s) and whole-step (25s)
          deadlines, and the post-inline body re-check against the 16 MiB
          sealed-frame budget. Each fetch worker runs under saferun.Recover: the
          goroutine parses fully attacker-controlled bytes off the HTTP handler
          goroutine, where an unrecovered panic would be a process-wide fatal.
        status: implemented
      - description: >
          Cost gating: fetching runs AFTER token-rate admission, the balance
          reservation, and the catalog check (consumer.go phase-2 call site). The
          pre-fetch reservation uses the greater of the raw-byte billing bound and
          the media-aware 300/1500-soft-token estimate, so a caller funded only for
          the short URL cannot drive egress. That floor is NOT an upper bound on
          real vision tokens, so once the media is inlined the reservation is
          re-taken against the post-inline byte bound
          (topUpReservationForInlinedMedia) — without it, settlement's
          2x-reservation overage clamp would silently absorb the shortfall and
          recompute the provider payout from the clamped total. Exclusive
          self-route skips the balance reservation, so resolveRemoteMedia first
          runs the self-route serve-ability check (selfRouteUnavailable) before any
          fetch — a caller with no linked/online/capable machine gets the
          self-route error without driving egress. The monetary reservation is
          refunded on fetch failure and on a failed top-up; token-rate admission is
          deliberately retained, matching other post-admission validation failures,
          so failed URL probes consume quota.
        status: implemented
      - description: >
          Sealed requests are never fetched: a sender-sealed payload carrying a
          remote media URL is rejected pre-billing with a clear error
          (gateRemoteMediaPreDispatch) — fetching would generate origin-observable
          egress correlated with a payload the sender chose to seal.
        status: implemented
      - description: >
          Consumer-facing fetch errors carry only generic messages (media_blocked,
          media_too_large, …). Coordinator logs record stable code/status fields
          only; request URLs, hosts, query strings, fragments, credentials, and
          wrapped network errors are never logged because presigned media URLs
          commonly contain secrets. The API cannot be used as a fine-grained
          internal-network oracle beyond the allowed/blocked bit.
        status: implemented
      - description: >
          Configuration integrity: every numeric bound is validated at boot
          (AppConfig.Check -> mediafetch.Config.Check), so an operator typo
          (MAX_PARTS=0, TIMEOUT_MS=-1) aborts startup instead of silently reverting
          to a default; NewResolver additionally clamps and WARNs for a
          programmatically supplied ServerConfig.MediaFetch, and WARNs whenever
          ALLOW_PRIVATE_IPS or ALLOW_NONSTANDARD_PORTS is on so a production
          process running without the full SSRF policy is visible in the logs.
        status: implemented
      - description: >
          Redirect chains cannot silently downgrade transport: a hop to http is
          refused when any earlier hop used https (redirectGuard), so a caller who
          supplied an https URL keeps confidentiality and integrity for the whole
          chain — a redirect's signed query never crosses the network in plaintext
          and an on-path party cannot substitute the bytes before they are inlined
          into the encrypted provider request. Response headers are capped at
          64 KiB (MaxResponseHeaderBytes): headers are read before any body budget
          applies and Go's 10 MiB default would let an attacker-owned origin force
          GlobalConcurrency x 10 MiB of buffering while every body stayed in budget.
        status: implemented
      - description: >
          Residual (accepted): the blocked/allowed distinction plus timing still
          leaks coarse reachability of public HTTP/HTTPS origins on ports 80/443
          from the coordinator's egress IP; origins learn the coordinator fetches
          on behalf of requests.
          Operators can disable the feature per deployment via
          EIGENINFERENCE_MEDIA_FETCH_ENABLED=false, which authoritatively restores
          the pre-dispatch 400. The switch is read at process start like every
          other coordinator env var, so an incident rollback requires recreating
          the container — editing the env file alone does not affect a running
          process. An earlier revision of this entry claimed a live per-request
          re-read; that was wrong (os.Getenv returns the environment captured at
          exec) and the code no longer pretends otherwise.
        status: implemented
      - description: >
          Residual (accepted): on the public route a funded caller whose model IS
          served by vision-capable providers, but whose providers are momentarily
          at capacity, drives the fetch and then receives a 429/503 from
          runInferenceAdmission with the monetary reservation refunded. Fleet
          ELIGIBILITY is already gated before any fetch (visionToolsFailFast ->
          HasVisionProviderForModel / HasToolCapableProviderForModel, which run
          ahead of the media gate), so only transient capacity is affected, and
          token-rate admission is charged before the fetch and never refunded, so
          repeat probing consumes the caller's ITPM quota. Duplicating the
          capacity preflight ahead of resolution was rejected deliberately: the
          admission gate needs the POST-inline traits (MinPrefixCacheProtocol is
          derived from body size), so a pre-fetch copy would either answer from
          the wrong body or have to be kept in sync with the real gate, and it
          would still race.
        status: accepted
      - description: >
          Residual (accepted): on the exclusive self-route path the pre-fetch
          serve-ability check uses the traits derived from the PRE-inline body, so
          a size-derived trait that only inlining raises (MinPrefixCacheProtocol,
          computed from body size) cannot be known before the fetch. An owner
          whose only machine is a protocol-0 build can therefore drive one capped
          fetch and then be rejected by runInferenceAdmission. Assuming the
          higher protocol floor pre-emptively was rejected: most inlined media
          stays far below the threshold, so it would falsely reject self-route
          requests that would have succeeded. The exposure is one fetch, bounded
          by the per-request byte caps, on a caller's own hardware.
        status: accepted
    open_findings: []
    severity: high
    detection_hint: >
      Any change that adds a coordinator-side fetch of a consumer-supplied URL
      outside coordinator/mediafetch, weakens ipAllowed (new allowlisted range,
      Is4In6 unmapping, IPv6 zone stripping, or transition-space denies removed),
      diverges host canonicalization from net/http's idna.Lookup profile, permits
      non-standard ports by default, allows an https to http redirect downgrade,
      unsets MaxResponseHeaderBytes, lifts DisableCompression, removes the shared
      read-time byte budget or the projected-inlined-size check, drops
      saferun.Recover from the fetch workers, lets the deduplication key rewrite
      the URL that is actually fetched, trusts the response Content-Type header,
      skips image-header dimension parsing, moves resolveRemoteMedia before the
      balance reservation, skips the post-inline reservation top-up, forwards a
      body to a provider that was captured before inlining, or fetches for sealed
      requests must be treated as high severity.

  # ── TB-003: Provider inline-video plaintext lifetime ─────────

  - id: T-050
    trust_boundary: TB-003
    stride: Information_Disclosure
    title: Inline video plaintext exposed through provider temporary files
    description: >
      The provider decrypts each inline video before inference. The pre-fix path
      wrote those bytes to FileManager.default.temporaryDirectory as
      vlm-<UUID>.mp4 because AVFoundation consumed a URL, then removed the file
      only when EngineV2VisionPrefill.prepare returned. A provider operator could
      watch and copy the live file despite the process-memory protections, and a
      crash, SIGKILL, or power loss could bypass the defer cleanup and leave the
      consumer's video on disk. Coordinator-to-provider transport encryption does
      not protect such an at-rest artifact.
    adversaries: [ADV-001]
    affected_assets: [A-001]
    affected_files:
      - provider-swift/Sources/ProviderCore/Inference/MediaIngest.swift
      - provider-swift/Sources/ProviderCore/Inference/EngineV2VisionPrefill.swift
      - provider-swift/Sources/ProviderCore/Service/ProcessLifecycle.swift
      - provider-swift/Sources/darkbloom/StartCommand+Modes.swift
      - libs/mlx-swift-lm/Libraries/MLXLMCommon/MemoryBackedVideoAsset.swift
      - libs/mlx-swift-lm/Libraries/MLXLMCommon/UserInput.swift
      - libs/mlx-swift-lm/Libraries/MLXVLM/MediaProcessing.swift
    mitigations:
      - description: >
          Inline MP4 and QuickTime bytes are retained by MemoryBackedVideoAsset.
          Its AVAssetResourceLoader delegate serves bounded byte ranges from Data
          over a private custom URL scheme, and UserInput.Video.memoryBacked owns
          the asset, loader, bytes, and serial delegate queue through frame
          sampling. No file or network URL is involved.
        status: implemented
      - description: >
          AVURLAssetReferenceRestrictions.forbidAll prevents the container from
          following external references. Provider media ingestion still accepts
          only base64 data:video/mp4 or data:video/quicktime payloads, applies the
          existing decoded-byte, duration, frame-pixel, video-count, and aggregate
          caps, and rejects http(s), file, and unsupported container MIME types.
        status: implemented
      - description: >
          Coordinator-connected foreground and standalone local startup each
          call ProcessLifecycle.acquireMediaServingLock exactly once. The shared
          seam first acquires the production single-instance lock, then removes
          regular files matching the exact legacy vlm-<UUID>.mp4 name. It runs
          outside ProviderLoop construction and scheduled-loop reconstruction,
          clearing crash-orphaned plaintext without touching unrelated or
          non-regular temporary-directory entries.
        status: implemented
      - description: >
          Provider and mlx-swift-lm tests force AVFoundation metadata and frame
          processing through the memory-backed asset, pin random-access range and
          cancellation behavior, verify MP4/QuickTime wire compatibility, assert
          that no vlm-*.mp4 file appears, lock the startup purge allowlist, and
          verify that the shared media-serving seam orders one housekeeping pass
          after successful lock acquisition and none after lock failure.
        status: implemented
    open_findings: []
    severity: high
    detection_hint: >
      Any inline-video path that calls Data.write, FileManager.createFile, or
      AVAsset(url:) with a file/http URL reopens this finding. Changes to
      MemoryBackedVideoAsset ownership, resource-loader ranges, reference
      restrictions, MIME support, cancellation, or startup cleanup require the
      focused provider and mlx-swift-lm media suites.

  - id: T-051
    trust_boundary: TB-001
    stride: Information_Disclosure
    title: System profiler tables and admin exports leak identity or content
    description: >
      The system profiler persists one prompt-free row per dispatched attempt
      (request_profiles) and one row per provider slot per minute (fleet_snapshots),
      exported via admin-key-gated NDJSON endpoints. A provider could try to smuggle
      free-form text through its profile object, a client could plant a
      spreadsheet-formula or PII string via X-Request-ID, or a hardware serial /
      stable identity could re-enter a bulk-export surface.
    adversaries: [ADV-002, ADV-004]
    affected_assets: [A-007, A-009]
    affected_files:
      - coordinator/api/profiler*.go
      - coordinator/registry/request_profile.go
      - coordinator/registry/fleet_sample.go
      - coordinator/store/profile_records.go
      - coordinator/store/postgres_profiles.go
    mitigations:
      - description: Provider profile rides the terminal message as raw JSON, is length-capped before any byte is read, decoded only after the terminal is fully processed, and persisted from a separate typed struct whose enums are closed and folded (no free-form string can reach a row).
        status: implemented
      - description: The persisted correlation id is always coordinator-minted; the client-supplied X-Request-ID never enters a row, and CSV writers prefix formula triggers.
        status: implemented
      - description: No serial, Secure Enclave key, account id, or stable hardware identity column exists in either table; only the session provider_id and catalog model id.
        status: implemented
      - description: Retention sweeps (14 d profiles, 30 d snapshots) bound the exposure window.
        status: implemented
    open_findings: []
    severity: medium
    detection_hint: >
      Any new column on request_profiles/fleet_snapshots, any new field on the
      provider profile wire object, or any new admin export must be reviewed
      against the closed-enum / no-identity rules in docs/architecture/system-profiler.md.

  # ── TB-011: Coordinator local disk ──────────────────────────

  - id: T-052
    trust_boundary: TB-011
    stride: Information_Disclosure
    title: Batch prompts and results sitting on coordinator disk are read outside the process
    description: >
      The batch lane is the only surface that stores consumer prompt content at
      all. An uploaded JSONL file, one sealed body per item, one sealed result per
      item, and the assembled output/error files sit on the coordinator's own
      filesystem for the life of a 24-hour batch and for seven days after it
      completes. Anything that can read that filesystem without going through the
      coordinator process — a disk image, a VM snapshot, a backup, a stray volume
      mount, an operator with root on the CVM host, or a Postgres dump if content
      ever leaked into a column — would obtain plaintext prompts and completions
      in bulk rather than one request at a time.
    adversaries: [ADV-002, ADV-003]
    affected_assets: [A-015, A-001]
    affected_files:
      - coordinator/store/sealedblob/blob.go
      - coordinator/api/batch_files.go
      - coordinator/api/batch_handlers.go
      - coordinator/api/batch_assembler.go
      - coordinator/api/batch_config.go
      - coordinator/batchlane/dispatcher.go
      - coordinator/store/batch_types.go
      - coordinator/store/postgres_batch.go
    mitigations:
      - description: Every stored object is NaCl-Box ciphertext under an X25519 key derived from the coordinator mnemonic with its own HKDF domain (eigeninference-coordinator-batchstore-v1), sealed with a fresh ephemeral key pair per write. A disk image alone yields no plaintext.
        status: implemented
      - description: Files are 0600 inside a 0700 directory and written temp-file + fsync + rename, so no truncated or world-readable blob is ever observable.
        status: implemented
      - description: Postgres is metadata only. batch_files, batches and batch_items carry no body, hash or prefix column; TestNoContentColumnsInBatchTables asserts that against information_schema.columns, and duplicate custom_ids are rejected in Go so a unique-index violation cannot print one into the server log.
        status: implemented
      - description: No log line, error string, metric or blob name carries a prompt, a result, a custom_id, a filename or a metadata value. Validation errors name a line number and a field; the offending value, the requested model, the endpoint and the content-part type are all withheld. Blob refs are coordinator-minted ids validated against [A-Za-z0-9_-]{1,128}, so consumer text can never become a path.
        status: implemented
      - description: Multipart uploads are streamed through mime/multipart's low-level Reader rather than ParseMultipartForm, which would spill an oversized part to a plaintext os.CreateTemp file before the handler could seal it.
        status: implemented
      - description: Plaintext exists only inside batchlane's runItem for the duration of one dispatch. It is never stored on the Dispatcher, never copied into an Outcome, and TestPlaintextIsNotRetained pins that the only on-disk copies are the two sealed blobs.
        status: implemented
      - description: Purge is bounded and layered. The uploaded file's blob is deleted as soon as POST /v1/batches has fanned it out; item input blobs are deleted at finalize; item results and assembled files are deleted at BatchOutputRetention (7 days) by the dispatcher's retention pass and PurgeExpiredBatchFiles; an hourly orphan pass removes item blobs no row references.
        status: implemented
      - description: A consumer may supply result_public_key (base64, exactly 32 bytes, strictly validated — a malformed key is a 400, never a silent fallback), after which results are sealed to that key and the coordinator can Raw but not Open them.
        status: implemented
      - description: The lane fails closed. Without a mnemonic, NewBatchBlobStore returns nil, every /v1/files and /v1/batches route answers 503 batch_unavailable, and startBatchDispatcher does not run, so prompts are never written unsealed.
        status: implemented
      - description: "Residual: the batch-store key is derived from the same mnemonic as the sender-encryption key, so one root secret compromises both."
        status: accepted
      - description: "Residual: the key is in coordinator memory and the blobs are on the coordinator's own disk, so root on the CVM host reads both. The seal defends against images, snapshots and backups, not against the host."
        status: accepted
      - description: "Residual: there is no key rotation. Rotating means redeploying with a new mnemonic, after which existing blobs are unreadable and open batches are lost."
        status: accepted
      - description: "Residual: EIGENINFERENCE_BATCH_DEV_INSECURE_KEY=true substitutes a process-local random key. It is logged as a WARN and is local-development only; set in production it would accept uploads that become unreadable at the next restart."
        status: accepted
      - description: "Residual: batch metadata, the uploaded filename and custom_id are consumer strings stored in Postgres by design. They are kept out of every log line and error message, but they are not sealed."
        status: accepted
    open_findings: []
    severity: high
    detection_hint: >
      Any new column on batch_files/batches/batch_items, any new log or error
      string in coordinator/api/batch_*.go or coordinator/batchlane/, any change
      to sealedblob's key derivation, file modes or ref validation, any change to
      the purge schedule or BatchOutputRetention, and any new consumer surface
      that reads a blob must be reviewed against this threat and against
      docs/architecture/batch-lane.md.
